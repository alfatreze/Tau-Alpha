# PHASE_F_SPEC.md section 10, step 1: synthesis-only proof of the two functionally
# inert pieces (MLAB migration, font ROM repack) before any Quartus fit is spent.
# Appended to the STAGED src/fpga/ap_core.qsf, after any base macro set + a SEED line.
# Both pieces change zero behaviour; the fit/synthesis RAM summary is the entire result.
set_global_assignment -name VERILOG_MACRO "TAU_MLAB_MIGRATE=1"
set_global_assignment -name VERILOG_MACRO "TAU_FONT_REPACK=1"
