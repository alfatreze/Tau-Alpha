# docs/PHASE_F_SPEC.md section 4/4.1, docs/AUDIT_TRAIL.md B-223: synthesis-only proof that
# Quartus still infers M10K/MLAB for BOTH regions of tau_main_ram.sv's split main-RAM design
# (32768 + 16384 words/lane = 192 KB) instead of falling back to registers, the way a *different*
# past restructuring of this exact memory once did ("Cannot convert all sets of registers into
# RAM megafunctions"). Appended to the STAGED src/fpga/ap_core.qsf, after any base macro set + a
# SEED line. quartus_map only -- no fit is spent by this check.
set_global_assignment -name VERILOG_MACRO "TAU_RAM_192K=1"
