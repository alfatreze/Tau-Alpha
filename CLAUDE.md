# Multi-Agent Workspace Control Blueprint

## 1. System Personas
- **Architect & Auditor:** Claude Code (Local Loopback over Proxy)
- **Code Implementer:** Aider (Direct to LM Studio Qwen-14B)
- **Adversarial Code Reviewer:** Codex (Local Git Tracking)

## 2. Shared Development Workflow
1. **Claude (Auditor)** must outline system changes, file layouts, and security rules here before code changes begin.
2. **Aider (Qwen-14B)** must read this file, execute the code modifications line-by-line, and make a clean Git commit.
3. **Codex** must run background checks against modified files to detect bugs, race conditions, or performance flaws.

## 2b. Session start (read before working)
0. **Starting new work rather than finishing something?** `docs/PHASE_F_SPEC.md` is the active plan (blit engine, M10K release, spectrum, the no-3D and kernel decisions, parked ideas). Its section 14 names the next item and gives enough detail to start cold.
1. `docs/SESSION_HANDOFF_2026-09-25_BLIT_TEST_ROOT_CAUSE.md` then `docs/CURRENT_STATUS.md` (latest, B-192/B-193: the whole B-166..B-191 "Blit Test hangs" investigation is root-caused and FIXED -- three functions (`bt_crumb()`/`bt_crumb_read()`, `blit_probe()`, `set_thumb_flat_build()`) used a raw CPU pointer into `0xA0000000`+small-offset, the draw engine's own framebuffer/guard region, which the CPU's real uncached SDRAM window does not cover (`PL_SDRAM_BASE`/`0xA0100000`, a full 1 MiB later); confirmed by JTAG ISSP disassembly and an A/B test ruling out the HarpMudd port, the ISSP tooling, and a cold-window theory. All three fixed, hardware-confirmed working. **A NEW, separate, NOT-yet-resolved anomaly (B-194): past all three fixes, Blit Test now runs far longer than its ~48s design duration without completing/hanging/resetting -- do not assume it is fixed end-to-end, read B-194 before touching this again.** Superseded but still correct: `docs/SESSION_HANDOFF_2026-09-24_BLIT_TEST.md` (B8/CLUT-blit done, ISSP built, the hang first isolated to CPU/firmware-side), `docs/SESSION_HANDOFF_2026-09-24.md`, `docs/SESSION_HANDOFF_2026-09-22_RELEASE_0.4.md`, `docs/SESSION_HANDOFF_2026-09-21_RELEASE_0.3.md`, `docs/SESSION_HANDOFF_2026-09-21.md`.
2. `docs/AUDIT_TRAIL.md`: A-NNN = SDRAM/UI/firmware series (ends A-137), B-NNN = PSRAM through the media library, Phase G, the Check and this release; log every hardware result with an evidence label.
3. Commit only your own files; never stage another session's uncommitted RTL/sim/PSRAM docs. Ask before any SD-card write and follow the backup / clear-catalog / verify-SHA-256 / eject procedure -- concrete checklist, exact cache filenames and known pitfalls (a stale catalog cache, a mis-rooted library index, an over-length core.json field): `docs/CARD_INSTALL_PROCEDURE.md`.

## 3. Strict Project Rules
- Never modify this repository's upstream tracking references.
- Always append an execution log entry to this file after finishing a coding turn.

## Subagent delegation policy

Before spawning a subagent (Task/Agent tool), check whether it's actually worth the overhead:
- Only delegate when the work is verbose/high-volume (test runs, log scanning, multi-file research,
  large doc fetches) or genuinely parallelizable. Small, quick lookups stay in the main context —
  a subagent's own startup cost (loading CLAUDE.md, skills, MCP tool defs) isn't worth it for trivial work.
- Never spawn a subagent just to "be thorough" — spawn one because its output would otherwise bloat
  the main conversation.

When delegating, write the subagent prompt to minimize its cost and the tokens it returns:
- Scope it tightly: state the exact question/deliverable, the specific files/paths/URLs it needs
  (not "explore the codebase"), and an explicit output format so it doesn't over-explain.
- Explicitly cap output: tell it to report back a short summary or the specific artifact needed —
  not full transcripts, full file contents, or step-by-step narration — since only the returned text
  re-enters the main context.
- Pick the cheapest model that can do the job: use `model: haiku` for mechanical/simple subagent tasks
  (formatting, running a command and reporting pass/fail, simple lookups), and reserve a stronger model
  for subagents doing real reasoning or judgment calls.
- Keep the spawn prompt itself short — everything in it enters the subagent's context immediately,
  on top of whatever CLAUDE.md/skills/MCP tools it auto-loads.
- Prefer one well-scoped subagent over several redundant ones; batch independent lookups into a single
  subagent call when they don't need separate context isolation.
- If a task doesn't clearly need isolation (verbose output, parallelism), just do it directly instead
  of delegating.

## AUDIT COMPLIANCE TARGETS
- **Code Style:** Ensure consistent naming conventions and formatting.
- **Security:** Regularly review for potential vulnerabilities.
- **Performance:** Optimize code for efficiency.

## Execution Log
- 2026-09-19 (Claude): docs only. Added `docs/A088_UPSTREAM_CHECKS.md` (A-088 next-run checks derived from HarpMudd upstream). No code, no upstream refs touched; upstream cloned to session scratchpad only.
- 2026-09-19 (Claude): docs only. Added `docs/UPSTREAM_MEMORY_AUDIO_KNOWLEDGE.md` (upstream SDRAM/PSRAM/audio knowledge; all other upstream features deferred until SDRAM, PSRAM and FPGA audio are settled). Font-base address verified in upstream `mp3_fb.sv` (`FONT_BASE = 25'h0100000`, word address = byte 2 MiB).
- 2026-09-19 (Claude): firmware/packaging. A-087 (source-word display), A-088 (datatable bridge base 0xF8000000 -> 0xF8002000; slot write confirmed on Pocket), A-089 (slot 5 parameters 0x22 -> 0x86; no effect). All firmware-only or packaging-only, same Quartus-verified A-080 RBF; each installed on the SD card with cache backup. Flush (0188) still times out and the save file is still zero. Details in `docs/AUDIT_TRAIL.md`.
- 2026-09-19 (Claude): firmware/packaging. A-090 (`TAU_LOG_TABLE_PROBE`): slot-5 table size/integrity, 10 s `0188` flush with timing, post-flush re-read; slot parameters back to 0x22. Same A-080 RBF; installed on the card with cache backup. Result pending. See `docs/AUDIT_TRAIL.md`.
- 2026-09-19 (Claude): docs only. Recorded A-090 Pocket result (slot table OK, `0188` flush unanswered at 10 s, second read timed out, file zero) and corrected `docs/A088_UPSTREAM_CHECKS.md` against upstream ROADMAP (0184 is not a proven persistence path; interact.json is).
- 2026-09-19 (Claude): firmware/packaging/tooling. Drafted A-091 (`TAU_LOG_INTERACT_PROBE`): diagnostic result published through interact.json persist (16 words, 31-bit encoding), no 0184/0188/slot 5; packager `--probe-a091`; `tools/decode_tau_diag_log.py --interact`. Built and packaged only; NOT installed on the card. See `docs/AUDIT_TRAIL.md`.
- 2026-09-19 (Claude): card. Installed A-091 on the Pocket SD card in place of A-090 (cache backup, SHA-256 verified). Result pending; Quit the core before removing the card so APF writes interact_persist.json.
- 2026-09-19 (Claude): docs. Recorded A-091 Pocket result: interact.json persist channel works; the screen (`W0`,`W7`,`WC`,`WF`) matches the decoded persist file (checksum 0x478E986A). Result-log path closed; SDRAM return-path fault unchanged.
- 2026-09-19 (Claude): docs only. Closed issue 019 (resolved by A-088 address fix + A-091 interact.json persistence); updated PROJECT_REGISTER, CURRENT_STATUS next gate, and marked DIAGNOSTIC_RESULT_LOG transport as superseded.
- 2026-09-20 (Claude): firmware/packaging/tooling. Reviewed A-060..A-079 and issue 018; drafted A-092 (`TAU_DISCRIMINATOR_PROBE`): firmware-only mailbox-vs-CPU-window discriminator publishing 14 raw words via interact.json; `--probe-a092`, `decode_tau_diag_log.py --interact --raw`. Built/packaged only; NOT installed on the card.
- 2026-09-20 (Claude): card. Installed A-092 in place of A-091 (cache backup, SHA-256 verified). Result pending; Quit the core before removing the card.
- 2026-09-20 (Claude): RTL/sim/docs. A-092 Pocket result decoded (writes land; reads lag one beat back-to-back). Added `sim/tb_tau_sdram_wb_return_regression.v` (`make test-rtl-sdram-wb-return`), which fails on the old adapter and reproduces the Pocket symptoms; fixed `src/fpga/core/tau_sdram_wb_adapter.sv` with a second release cycle (A-093). `make test-rtl` and `make test-host` pass. Quartus build and Pocket run pending; issue 018 root cause recorded.
- 2026-09-20 (Claude): docs/skill only. Added project skill `.claude/skills/analogue-pocket-dev/`: SKILL.md router + references (json-files, host-target-commands, hardware/video/audio, chip32, sd-packaging, changelog, template-and-examples) + `docs-snapshot/` (full text of all analogue.co developer docs pages) + `repo-src/` (template core_top/core_bridge_cmd, chip32 example/arch). Finding: template target FSM never issues 0188/0181/0185/0152. No code or upstream refs touched; open-fpga repos cloned to session scratchpad only.
- 2026-09-20 (Claude): VM. Staged `/home/taualpha/tau-local/phase2-adapter-fix-a093-20260920` (A-093 adapter fix; macros TAU_PHASE2_WINDOW=1 and TAU_PHASE2_MUX_PROBE=1, as A-080) and launched `make fpga` at 00:17:54 WEST (PID 28654, log quartus-a093.log). Result pending; recorded in AUDIT_TRAIL A-093.
- 2026-09-20 (Claude): docs/skill only. Extended `.claude/skills/analogue-pocket-dev/` with an evidence-graded knowledge base (`references/knowledge-base/`: 21 entries, approaches, open questions, resources, sources.json) and `scripts/kb.py` (validate/index/new/promote/stale) + `scripts/refresh.py` (docs/repo drift). Community repos (agg23 utils+wiki, NES, LiteX, janisc NGPC, Paprium, Gateman) cloned to session scratchpad only. Key lead: nonvolatile flush size comes from the core size table at slot POSITION (KB-001); template never issues 0188 (KB-007). ~/.claude/skills entry is now a symlink to the repo copy. No code or upstream refs touched.
- 2026-09-20 (Claude): Quartus/packaging. A-093 build finished Successful (44m03s, 0 errors, +1.106 ns setup, +0.111 ns hold, RBF sha256 e16ffe9d...4d9d); copied to work/diagnostics/sdram-cpu-probe-a093/fpga; added packager `--probe-a093` (pairs it with the A-091 matrix ROM); bundle built, NOT installed on the card.
- 2026-09-20 (Claude): card. Installed A-093 in place of A-092 (cache backup, SHA-256 verified). Result pending; Quit the core before removing the card.
- 2026-09-20 (Claude): docs. Recorded A-093 Pocket PASS (183 checks, 0 failures; screen and interact.json record agree, checksum 0x18511A0D); issue 018 resolved; scope limited to the uncached CPU-window data path.
- 2026-09-20 (Claude): docs/KB/firmware. Committed A-093 (three commits). Used the project skill `analogue-pocket-dev`: added KB-022..KB-025 (hardware-validated from A-088/A-090/A-091/A-093), appended notes to KB-001/004/007/008/021 and OQ-1/2/6, `kb.py validate`/`index` pass. Drafted A-094 (`TAU_LATENCY_PROBE`, uncached SDRAM window cost, firmware-only, same A-093 RBF); built/packaged, NOT installed. Promotion gates listed in docs/CURRENT_STATUS.md.
- 2026-09-20 (Claude): card. Installed A-094 in place of A-093 (cache backup, SHA-256 verified). Later A-093 persist file (00:51) decoded: 183 checks, 0 failures, checksum 0x1851CA53 (independent confirmation of a repeat run). Could not register the analogue-pocket-dev skill into the running session (needs a new session). A-094 result pending; Quit the core before removing the card.
- 2026-09-20 (Claude): docs. Recorded A-094 Pocket result: uncached SDRAM access ~48-50 cycles net (0.8 us) at 60 MHz, worst single access ~360 cycles, 0 mismatches; stride-write speed-up unexplained. Next: count accesses per candidate cold buffer.
- 2026-09-20 (Claude): docs. Recorded A-094 Pocket result: uncached SDRAM access ~48-50 cycles net (0.8 us) at 60 MHz, worst single access ~360 cycles, 0 mismatches; stride-write speed-up unexplained. Next: count accesses per candidate cold buffer.
- 2026-09-20 (Claude): docs. Committed A-094 (firmware+docs; `.claude/` left untracked, it holds verbatim Analogue doc snapshots). Added A-095 static access-count analysis: pl_text/pl_off/pl_order (13 KiB) cost tens of ms per playlist load; art_acc would add ~0.8-0.9 s to a full cover decode. Estimates only; user decision pending on 13 KiB first step vs reaching 24 KiB.
- 2026-09-20 (Claude): firmware measurement, no product change. A-096: built a throwaway 7-row settings menu (TAU_SETTINGS_PROTO; kept in work/diagnostics/settings-size-proto/, tracked files restored). It adds 2,996 B and fails the link by 608 B (reproduces the old rejection); with the 13 KiB playlist move it would leave ~12.7 KiB spare, so the 24 KiB target is not needed for it. An accidental rebuild of dist/Assets/tau/common/tau.rom was reverted with git checkout.
- 2026-09-20 (Claude): firmware/tooling. Committed A-095/A-096 docs (a2183b8). Drafted A-097 soak (`TAU_SOAK_PROBE`): matrix + random write/read + block verify forever on the A-093 RBF, cumulative record via interact.json; `--probe-a097`, decoder `--interact --soak`. Built/packaged, NOT installed.
- 2026-09-20 (Claude): card. Installed A-097 (soak) in place of A-094 (cache backup, SHA-256 verified). Result pending; leave running 30-60 min, then Quit before removing the card.
- 2026-09-20 (Claude, weekly refresh): docs/skill only. Drift: 0 doc pages changed; 1 repo moved (openfpga-library, metadata only), Codeberg tutorials 404. Discovery: 3 new entries (KB-026..028, community-reported), KB-004 promoted to source-verified (3 cores pair a nonvolatile slot `address` with core-served bridge reads), 4 repos added to sources.json, OQ-10 added. kb.py validate passes; nothing stale. No code or upstream refs touched.
- 2026-09-20 (Claude): docs only. Added `docs/PSRAM_IMPLEMENTATION_PLAN.md`: analysis of PSRAM_EVALUATION_PLAN.md against the repo (core_top cram pins, decoder map, A-093/A-094/A-096 results, agg23 reference controller) with corrections (address width, WAIT, stale save-record path, missing I/O constraints, guard at CPU-word granularity), a P0-P5 work list from A-098, and owner decisions. No code touched.
- 2026-09-20 (Claude): RTL/sim/docs, no hardware. PSRAM P0/P1 (B-001): `docs/PSRAM_TIMING_CONTRACT.md` (datasheet not fetchable, values OPEN), `tools/check_psram_idle.py`, `tau_psram_async.sv`, `tau_psram_bus.sv`, strict chip model + two testbenches + mutation target in `make test-rtl`; `make test` passes. Not wired into core_top/CPU decode/firmware. P2 blocked on the datasheet. Owner delegated decisions recorded in `docs/PSRAM_IMPLEMENTATION_PLAN.md` section 0.
- 2026-09-20 (Claude): RTL/sim/docs, no hardware. B-002: read the AS1C8M16PL datasheet (`docs/vendor/DOC012312972.pdf`); rewrote `docs/PSRAM_TIMING_CONTRACT.md` with page-referenced values; found and fixed a real read-timing violation in the B-001 controller (sampled ~17 ns after OE# low vs tOE 20 ns; `T_ACC` 6 -> 8); model and mutation tests strengthened. `make test` passes. P2 unblocked; remaining unknowns are hardware questions.
- 2026-09-20 (Claude): docs. Recorded A-097 soak Pocket PASS: 705,160 passes / 264,435,000 checks / 30:15 / 0 failures, pass 0.85-3.9 ms; screenshots and persist record agree. Covers only 2-3 MiB, no audio. Next: A-100 full-window coverage + CRC + counters.
- 2026-09-20 (Claude): firmware/tooling. Drafted A-100 (`TAU_FULL_PROBE`): whole-window address-line test (26 addresses, every line 2..25), 1 MiB CRC x3 with concurrent framebuffer drawing, access-time and draw-stall counters; `--probe-a098`, decoder `--full`. Built/packaged, NOT installed.
- 2026-09-20 (Claude): card. Installed A-100 (coverage) in place of A-097 (cache backup, SHA-256 verified). Result pending; run once (a few seconds to a minute), photograph, Quit before removing the card.
- 2026-09-20 (Claude): docs. Recorded A-100 coverage Pocket PASS: 52 address-line checks (every line 2..25) and 3x1 MiB CRC rounds under concurrent drawing, 0 failures, worst access 360/350 cycles, draw stalls 0. Next: probe-free product RTL on several seeds, then contention test. Only my code files to be committed (AUDIT_TRAIL/CLAUDE.md also hold the PSRAM session's uncommitted entries).
- 2026-09-20 (Claude): VM. Committed everything except `.claude/` and `docs/vendor/` (third-party doc snapshots / vendor datasheet PDF) in four commits. A-101: staged `/home/taualpha/tau-local/phase2-product-a101-s{1,2}-20260920` (only TAU_PHASE2_WINDOW=1, SEED 1/2) and launched both `make fpga` runs in parallel at 02:36 WEST; seeds 3 and 4 to follow. Results pending.
- 2026-09-20 (Claude): firmware/tooling. A-102: `TAU_SDRAM_STRESS_WINDOW` (`fw/build.sh player-stress-window`) drives CPU-window traffic (3 rates) beside real playback with HUD R/M/U/S; window preflight; packager `package_sdram_stress.py --window`. Found the Phase 1 stress ROM no longer links (624 B heap gap): `fw/link.ld` now `PROVIDE(_min_heap=1024)`, stress targets use 128; product build verified byte-identical. Built/packaged with the A-093 RBF, NOT installed. A-101 builds still running.
- 2026-09-20 (Claude): docs/skill only. Published the skill as public repo github.com/alfatreze/analogue-pocket-dev-skill (MIT; Analogue docs/APF RTL and project-private KB entries excluded via .gitignore, fetched/kept locally). Master is now the git checkout at ~/.claude/skills/analogue-pocket-dev; `.claude/skills/analogue-pocket-dev` here is a symlink to it (old copy backed up in the session scratchpad). Weekly task repointed; it leaves changes uncommitted. No code or upstream refs touched.
- 2026-09-20 (Claude): test assets. Local search found no long 320 kbps CBR/48 kHz music; installed LAME 4.0 via Homebrew and generated two ID3v2.3-tagged 320 kbps CBR files (44.1 kHz with 1400 px cover, 48 kHz with 455 px cover) from four local Nausicaa tracks plus a playlist with VBR/128 kbps controls in `work/test-music/tau_sdram_wst/common/` (README there). Verified by header scan, full decode and tools/library_check.py. Nothing downloaded from the web; not copied to the card.
- 2026-09-20 (Claude): VM. A-101 seeds 1 and 2 finished Successful: 300/308 RAM blocks, TNS 0, worst hold +0.115/+0.105 ns, worst setup +0.443/+0.524 ns (probe build had +1.106); RBFs copied to work/diagnostics/sdram-product-a101/s1|s2. Launched seeds 3 and 4 at 03:36 WEST. Pick after those finish.
- 2026-09-20 (Claude): packaging. Repackaged the A-102 window-stress core with the A-101 seed-1 product-candidate RBF (raw 89d7fd63...4256, rbf_r 898210a6...1b6b); ROM unchanged (d11309cc...0663). Bring-up only; final seed after seeds 3-4. Not installed on the card.
- 2026-09-20 (Claude): card. Installed the A-102 window-stress core (seed-1 RBF, SHA-256 verified) in place of the A-100 probe, and copied the five test tracks + playlist.m3u to Assets/tau_sdram_wst/common/ (byte-compared, 67 MB). Old Phase 1 stress core and others untouched. Result pending.
- 2026-09-20 (Claude): firmware. User's first A-102 session: no audible issue, one underrun seen, `S` not visible (HUD line clipped at 360 px). Reworked the window-mode HUD (counters first, S in ms); new ROM dc8eeeb4...c17a, repackaged with the seed-1 RBF, NOT yet on the card. Slip: an unintended `player-stress` build overwrote work/diagnostics/sdram-stress/tau.rom; restored from the package copy (1860455e...). Underrun context still needed.
- 2026-09-20 (Claude): card. Replaced the ROM on the card (tau_sdram_wst/common/tau.rom) with the new-HUD build dc8eeeb4...c17a (byte-compared); old ROM backed up; RBF and music unchanged.
- 2026-09-20 (Claude): analysis+firmware. Read 12 A-102 screenshots: no mismatch, M 362-367 (idle bound), S 0, no audible issue; but levels 2-3 were loop-limited to ~4.4k ops/s (pass times 50-70 s). Screenshots stop the track, so underrun counts (up to 13) are likely restart artefacts. Reworked the pump (true 16k/40k/72k ops/s, up to 32 ops per pass), HUD now E/L underrun split + achieved K rate; ROM rebuilt (155,116 B), repackaged, NOT yet on the card.
- 2026-09-20 (Claude): card. Replaced the A-102 ROM on the card with the rate-accurate/E-L-split build 59106130...be71 (byte-compared); previous ROM and indexes backed up. RBF and music unchanged. Result pending.
- 2026-09-20 (Claude): VM. A-101 seeds 3 and 4 finished Successful (setup +0.390/+0.464 ns, hold +0.116/+0.123 ns, 300/308 RAM blocks). All four seeds closed timing; seed 4 selected by the pre-set rule (best hold, beats A-093's +0.111). RBFs in work/diagnostics/sdram-product-a101/s1..s4. Stress core on the card still uses seed 1; repackage with seed 4 for the final contention gate when the user agrees.
- 2026-09-20 (Claude): analysis+firmware. Read the 12 A-102 session-3 screenshots (tracks 1-3, R0-R3): no mismatch, M 362 constant, S 0, no audible issue, late underruns only on track 1 coinciding with STOPPED/resume screenshots; achieved K only 3-13k ops/s (pump called from the FIFO-wait loop). Reworked pump (R1 paced 16k, R2/R3 unpaced 8/32-op bursts) and switched underrun classification to decoded frames since flush; ROM 55384a55...bc20a built and repackaged, NOT on the card.
- 2026-09-20 (Claude): card. Repackaged the A-102 stress core with the A-101 seed-4 RBF (raw ed34a6bc...90eb, rbf_r 2e9aaf0e...3cd7) and the burst ROM (55384a55...bc20a); replaced both files on the card, byte-compared; seed-1 RBF, old ROM and indexes backed up under seed4-swap/. Result pending.
- 2026-09-20 (Claude): docs. Recorded A-102 session with the seed-4 RBF + burst ROM: 4 tracks x R0-R3, L0 (no late underruns) everywhere, no mismatch, S0, worst window access 366-373 cycles, K up to 22.6k ops/s (CPU-idle-limited, ~6% CPU); realistic-load pass, saturation/FLAC/cold-boot repeat open.
- 2026-09-20 (Claude): handoff. Wrote docs/SESSION_HANDOFF_2026-09-20.md (state, rules, VM/card procedures, hashes, open gates), corrected stale audit-entry titles (A-091..A-102), committed and pushed. KB: added KB-011/021 notes; a Tau session accidentally overwrote the bodies of KB-026/027 (weekly-refresh entries) and the repair was blocked pending user decision; KB-030/031 are empty stubs (text is in the handoff doc).
- 2026-09-20 (Claude): docs only. Added `docs/PLAYLIST_SDRAM_MOVE_SPEC.md` and AUDIT_TRAIL A-103 (design of the first data move; nothing built). KB-026/027/030/031 verified intact, no repair needed. Card, VM untouched.
- 2026-09-20 (Claude): docs only. Verified the card (14/14 SHA-256 match) and read 8 screenshots of the cold-boot A-102 repeat with seek/pause/track change: L0, S0, M365-373, no failure. Recorded as A-104. Nothing written to the card.
- 2026-09-20 (Claude): firmware/packaging. A-105: playlist buffers (13,312 B) moved to SDRAM behind TAU_PL_SDRAM (default off): .sdram NOLOAD section at 0xA0100000, run-once window preflight in pl_load, feature-off fail-safe. Product ROM byte-identical (b365dc2a...); player-sdram-pl ROM 63e605e9...; heap gap 16,368 B. Packager --playlist-sdram with seed-4 RBF; bundle built, NOT installed. make test-host passes.
- 2026-09-20 (Claude): card. Installed A-105 (alfatreze.TAU_PLSDRAM + music) beside existing cores; 14/14 SHA-256 match, music diffed identical, indexes backed up and cleared. Result pending.
- 2026-09-20 (Claude): docs. Read 5 screenshots of the A-105 playlist-in-SDRAM core: playlist loaded from SDRAM, names/order/shuffle/seek/cover all correct, no audio issue reported. Recorded as A-106 (5-track list only; large-list and no-window negative tests still open).
- 2026-09-20 (Claude): tooling/docs. A-107: tools/make_large_playlists.py + three large-playlist test lists (240 fits; 300 clipped by count; 300 clipped by text) in work/test-music/tau_plsdram_large with modelled expectations. Not copied to the card.
- 2026-09-20 (Claude): card. Copied the A-107 large-playlist lists (3 .m3u + README) to Assets/tau_plsdram/common/; byte-compared. Result pending.
- 2026-09-20 (Claude): docs. Read 4 screenshots of the large-playlist tests (240 fits; 256 by count; 188 by text with the cut last name): all match the model. Recorded A-108; row 1 (no-window RBF) is the remaining gate.
- 2026-09-20 (Claude): firmware/packaging. A-109: found row 1 needs a rev-23 no-window RBF (interlock rejects rev 22); built fault-injection ROM (TAU_PL_SDRAM_FAULT, 00b17a68...) and packaged core TAU_PLSDRAMF with the seed-4 RBF, NOT installed. Normal SDRAM ROM and product ROM unchanged. True no-window RBF build not launched.
- 2026-09-20 (Claude): card+VM. A-109: installed fault-injection core TAU_PLSDRAMF on the card (14/14 SHA-256, indexes backed up/cleared). A-110: staged /home/taualpha/tau-local/nowin-a110-20260920 (A-101 s4 minus window macros and seed) and launched make fpga at 12:13 WEST. Results pending.
- 2026-09-20 (Claude): docs. Read 4 screenshots of the fault-injection core: playlist refused (idle screen, LOADING PLAYLIST then none, NO PLAYLIST LOADED on Select), single-file playback fine. Recorded A-111; NO SDRAM PLAYLIST toast not photographed. A-110 build still running.
- 2026-09-20 (Claude): VM/packaging. A-110 no-window rev-23 build finished Successful (300/308 RAM, slacks positive, RBF 743f9fdd...); packaged with the normal SDRAM-playlist ROM as TAU_PLSDRAMN (packager --nowin), NOT installed.
- 2026-09-20 (Claude): card. Installed A-110 no-window core TAU_PLSDRAMN (14/14 SHA-256, one track + playlist.m3u, indexes backed up/cleared). Result pending.
- 2026-09-20 (Claude): docs. Read 3 screenshots of the no-window core: playlist refused, playback fine, no side effect (row 1 pass). Code review found that TAU_PHASE2_WINDOW alone still instantiates the probe and draws the red/green top strip, so the A-101 'product' RTL is not probe-free; corrected the earlier 'fixed UI element' reading. Recorded as A-112; overlay split + re-fit proposed, not started.
- 2026-09-20 (Claude): RTL/docs. A-113: split TAU_PHASE2_PROBE (controller debug taps, probe, red/green overlay) from TAU_PHASE2_WINDOW in core_game.vh and sdram_fb.sv; Makefile controller-probe sim adds the probe macro. make test-rtl passes; quartus_map (window only) succeeds with the probe absent (7,470 regs vs 7,688). Not fitted, not on hardware; multi-seed fit and gate re-run pending.
- 2026-09-20 (Claude): committed A-113 (e67f93a, bf56f20; not pushed). VM: staged probefree-a114-s1/s2 (window only, seeds 1 and 2) and launched both make fpga at 15:05 WEST. Results pending.
- 2026-09-20 (Claude): docs only. Added `docs/ARCHITECTURE_ROADMAP.md`: orders PSRAM, measurements, hardware audio, media library, GPU, cached-window gate and 720 (last); corrects that the CPU already has caches and the draw engine is the GPU base. No code, card or VM touched.
- 2026-09-20 (Claude): firmware/tools. A-115: grouped in-app settings (fw/settingsui.inc; Start opens; Start-stop removed in that build) behind TAU_SETTINGS_UI (default off), player-settings target (needs TAU_PL_SDRAM; ROM 156,816 B, heap gap 11,984 B), packager --settings, 4 snapshot fixtures. Product and SDRAM-playlist ROMs unchanged; make test-host passes. Bundle built, NOT installed.
- 2026-09-20 (Claude): card. Installed A-115 TAU_SETTINGS (14/14 SHA-256) with the base TAU media copied (not moved; diffed identical); removed TAU_PLSDRAM/PLSDRAMF/PLSDRAMN after verified local backups; kept base TAU, WSTRESS, older STRESS/DIAG. Two aborted attempts logged in AUDIT_TRAIL. Result pending.
- 2026-09-20 (Claude): firmware/tools. A-116: playlist and settings now full screen (shared ov_frame, PL_UI_* 12 rows), fb_rect/fb_copy_span/fb_char held while an overlay is up (FB_HELD), settings choice lists (Colour swatches, Meter grey-placeholder thumbnails, EQ/Repeat/Blank radio, A selects), 9 settings + rewritten playlist fixtures. New ROM hashes (product 661c5936, SDRAM-pl 02fa0499, settings 78cb7582); settings bundle repackaged, NOT installed.
- 2026-09-20 (Claude): card. Replaced the TAU_SETTINGS ROM with the A-116 build (78cb7582...; A-115 ROM backed up); 14/14 SHA-256 match, media identical, indexes backed up/cleared. Result pending.
- 2026-09-20 (Claude): docs/firmware. A-117: read 12 A-116 screenshots (full-screen playlist, colour/repeat lists, EQ/volume all OK; screenshot combo does not toggle Start). Fixed the settings-over-playlist overlap (set_input closes the playlist); settings ROM 7f81d6f1..., bundle repackaged, NOT installed. Diagnostics-menu proposal given to the user.
- 2026-09-20 (Claude): firmware/tools. A-118: Diagnostics group (Info page, 11 read-only live values, TAU_DIAG_MENU, 1 s row refresh) plus the overlap fix in the settings ROM d0b5a32b... (156,252 B, heap gap 12,512 B); SDRAM-pl ROM now 82fdb70c (window-read timing); product ROM unchanged; 2 new fixtures. Save report deferred (4 free persist words). Bundle repackaged, NOT installed - card not mounted.
- 2026-09-20 (Claude): firmware/card. A-119: TAU_DIAG_MENU renamed TAU_DIAG_INFO (part of the release-style settings build), TAU_DIAG_TESTS reserved for the new Diagnostic Build (player-diagnostic, packager --diagnostic, bundle built, not installed); build.sh enforces min heap gap. Installed the A-118 settings ROM d0b5a32b... on the card (A-116 ROM backed up; 14/14 SHA-256). Result pending.
- 2026-09-20 (Claude): VM. A-114 fits finished Successful (both 300/308 RAM, no negative slack): seed 1 setup +0.158/hold +0.124, seed 2 setup +0.664/hold +0.124 (RBF 551e5a76...). Seed 2 chosen as the probe-free product-window candidate; RBFs copied to work/diagnostics/sdram-probefree-a114. Gate re-runs on Pocket pending.
- 2026-09-20 (Claude): card. Installed the probe-free seed-2 RBF (cb15310a...) into TAU_SDRAM_WSTRESS and TAU_SETTINGS (ROMs unchanged; 14/14 SHA-256 each; old RBFs and indexes backed up; indexes cleared). Gate re-runs on Pocket pending.
- 2026-09-20 (Claude): docs. Read the Info-page screenshot from the card (A-118 ROM on the old seed-4 RBF): all 11 values render and agree with the build (rev 4D503317, window OK, free RAM 12512 B, art decode 2.6 s of a 3.0 s load). Recorded A-120. The seed-2 probe-free RBF is not yet run.
- 2026-09-20 (Claude): docs. Read 10 screenshots of the window-stress core on the probe-free seed-2 RBF: strip gone, L0/M372/no failure text, but 1.2x speed was on for the first four frames (K 1.4k, S 1 ms at R3) and E is higher than A-102. User reports errors and poorer audio; not reproduced in the frames. Recorded A-121; A/B on old vs new RBF proposed.
- 2026-09-20 (Claude): firmware. A-122: 1.2x hold-A gesture compiled out where the settings menu exists (Speed stays in Settings > Playback); product ROM unchanged, settings/diagnostic ROM 448a49dc... (heap gap 13,312 B); bundles repackaged with seed-2 RBF, NOT installed. A/B RBF test postponed by the user.
- 2026-09-20 (Claude): docs only. B-003: reviewed later updates and skill changes against the PSRAM plan; added section 7 (12 considerations), macro split, next-free-audit-id policy, P5 candidate update. No code, card or VM.
- 2026-09-20 (Claude): card. Replaced the TAU_SETTINGS ROM with the A-122 build (448a49dc...; previous ROM and indexes backed up; 14/14 SHA-256). Result pending.
- 2026-09-20 (Claude): tooling/packaging. A-124: soak and coverage ROMs rebuilt (byte-identical to the passing ones) and packaged with the probe-free seed-2 RBF via the new --rbf option; bundles in work/diagnostics/sdram-cpu-probe-a097-pf and a100-pf, NOT installed. User reported the A-122 ROM tests fine.
- 2026-09-20 (Claude): card. Installed the soak (TAU_SDRAM_PRB97) and coverage (TAU_SDRAM_PRB100) cores with the probe-free seed-2 RBF (13/13 card files SHA-256-identical each; indexes backed up/cleared). Results pending.
- 2026-09-20 (Claude): firmware. A-125: Diagnostic Build Phase 2, Tests page (window test 89 checks at scratch 8 MiB, read/write cycles, playlist check, clear counters); ROM 9580c8e9... (heap gap 10,288 B); settings ROM now bd6a7700 (+8 B); product and SDRAM-pl unchanged; fixture settings-tests; SD read speed dropped; bundle repackaged, NOT installed.

- 2026-09-20 (Claude): docs/tools. PSRAM work now uses its own audit series **B-NNN** (B-001 = former A-098, B-002 = A-099, B-003 = A-123) so it can never collide with the A series; `tools/check_audit_trail.py` checks both series. No code touched.
- 2026-09-20 (Claude): card. Installed the Diagnostic Build (TAU_DIAGNOSTIC, ROM 9580c8e9..., 14/14 SHA-256, media copied from base TAU) and removed the superseded TAU_SDRAM_STRESS and TAU_SDRAM_DIAG after verified backups. Kept TAU, TAU_SETTINGS, WSTRESS, PRB97, PRB100. Result pending.
- 2026-09-20 (Claude): docs. Read the Diagnostic Build Tests screenshot: WINDOW TEST PASS 89, read 48/56/330, write 31/38/313, PLAYLIST CHECK PASS 13, counters cleared. Recorded A-126; agrees with the standalone probes. Working tree has uncommitted RTL edits (core_game.vh, core_top.v, mp3_soc.v) that are not mine.
- 2026-09-20 (Claude): firmware. A-127: Diagnostic Build Phase 3, stress pump (levels off/R1-R3) and timed soak (5-60 min) in Diagnostics > Stress with a live status page, reusing the A-102 pump; pump region moved to 2-3 MiB (playlist at 1 MiB); Select+X chord off where the menu exists. ROM 580bb638... (heap gap only 4,384 B). Settings ROM now bc26aeba; product and SDRAM-pl unchanged; 4 new fixtures; bundle repackaged, NOT installed.
- 2026-09-20 (Claude): RTL/firmware/tools/sim, no hardware. B-004 (PSRAM P2): `tau_psram_probe.sv` behind `TAU_PSRAM_PROBE` (mp3_soc `xm_*` expansion MMIO 0x88-0xA8, `docs/MMIO_ALLOCATION.md`), `fw/psram_diag.c` (`fw/build.sh psram-diag`), hash-locked `tools/package_psram_diagnostic.py`, decoder `--interact --psram`, and tests in `make test` (mailbox, real-CPU firmware-in-the-loop with an injected fault that must be caught, decoder, static pin check). Controller `T_ACC` 8 -> 9 after an I/O-delay review. Quartus VM build NOT launched and nothing installed: awaiting approval.
- 2026-09-20 (Claude): card. Replaced the TAU_DIAGNOSTIC ROM with the Phase 3 build (580bb638...; previous ROM and indexes backed up; 14/14 SHA-256). No cores removed: PRB97, PRB100 and WSTRESS are still the reference for the pending gates. Result pending.
- 2026-09-20 (Claude): VM. B-005: registered `tau_psram_async.sv`/`tau_psram_probe.sv` in `ap_core.qsf`; staged two fresh snapshots (`psram-probe-b004-s1/s2-20260920`, source hashes match local except the appended `TAU_PSRAM_PROBE=1` + SEED lines) and launched detached `make fpga` at 18:22 WEST after owner approval (logs `quartus-b004-s1/s2.log`). Result pending; nothing installed.
- 2026-09-20 (Claude): VM/packaging. B-006: both PSRAM-probe fits Successful (seed 1 setup +0.385/hold +0.033, seed 2 +0.642/+0.068; RAM blocks 300/308, DSP 11, no new memory); seed 2 RBF 142cd354...9de8 copied to `work/diagnostics/psram-diag/fpga`, bundle packaged hash-locked, NOT installed. Finding: the CRAM pad registers were not packed into I/O cells (no FAST_*_REGISTER assignments, no constraints); recommend adding them and rebuilding before P3. Awaiting a decision.
- 2026-09-20 (Claude): RTL/VM. B-007: reworked the PSRAM monitors so the CRAM pin registers have a single load (needed for I/O-cell packing), added `tools/psram_probe_qsf_append.txt` (probe macro + FAST_*_REGISTER on all CRAM pins, staged qsf only), `make test` passes; staged `psram-probe-b007-s1/s2-20260920` and launched detached builds at 19:38 WEST after owner approval. Result pending; B-006 bundle not installed.
- 2026-09-20 (Claude): VM/packaging. B-008: B-007 fits Successful on both seeds (seed 2 setup +1.012/hold +0.121, seed 1 +0.482/+0.083; RAM 300/308). I/O packing: DQ input + output-enable registers and all 14 control outputs packed; DQ output data and cram_a not ("clear and load" conflict, 44 warnings), judged not worth another build (>=28 ns margin), fix deferred to P4. Seed 2 RBF 8e9d9c16...873d packaged hash-locked; B-006 bundle archived; NOT installed.
- 2026-09-20 (Claude): docs/skill. Wrote local KB-036 (Quartus I/O-register packing rules from B-006/B-008), kept community-reported; kb.py validate/index pass. No code touched.
- 2026-09-20 (Claude): card. B-009: installed the PSRAM diagnostic (B-008 seed 2 bundle) additively on `Pock`: index backup verified, 13/13 files SHA-256-identical, five indexes cleared, metadata junk removed, unmounted. Predictions recorded (CRCs D0 833D7446 D1 4BF5A918 D2 ECE394E1 D3 8B21EF3F, CHECKS 1048704). P3 runs need the Pocket; results pending.
- 2026-09-20 (Claude): Pocket. B-010: PSRAM diagnostic P3 start C1 PASS on default and slow timing (all four die CRCs, 1,048,704 checks, 0 failures, guard hit, TO/CE clear); saved interact record decodes to the same values and every B-009 prediction held. Established defaults-only operation and a correct read path at T_ACC=9 on this unit; margin not yet known. 1 of 10 starts done; card read-only this time, unmounted.
- 2026-09-20 (Claude): Pocket. B-011: PSRAM diagnostic P3 complete, 10 of 10 starts (5 cold, 5 warm as reported) PASS on default and slow timing, all four die CRCs correct every time, zero errors; final saved record (run 3, 6,292,200 ops) decodes to PASS and matches the photo; install files unchanged; P3 exit gate met (bounded diagnostic access only). Read-timing margin still unknown. Card read-only, unmounted. P4 needs approval and a new build.
- 2026-09-20 (Claude): docs/skill. Wrote local KB-037 (Pocket PSRAM async on power-up defaults, hardware-validated from B-010/B-011); kb.py validate/index pass. No code touched.
- 2026-09-20 (Claude): RTL/firmware/tools/VM. B-012: PSRAM read-timing margin experiment. `PSRAM_T_ACC` macro (default 9) + read-only index in PS_CFG[15:8], firmware modes X/Y (read +1/+2) and index display/record, decoder fields, packager `--variant`; decoder verdict bug (word-4 high bits read as firmware timeout) found by its unit test and fixed; `make test` passes (25). Staged and launched two builds (T_ACC 7 and 6, seed 2) at 22:37 WEST after owner approval; predictions recorded (model: idx 6,7 FAIL, 8,9 PASS; if tAADV counts from ADV# falling, 7 passes). Result pending.
- 2026-09-20 (Claude): docs. Read the gate results on the probe-free seed-2 RBF: coverage PASS (52 lines, 3 CRC rounds, 0 fails, 359/350), soak PASS (32:00, 279,795,000 checks, 0 failures), in-menu soak PASS (29.4 M ops, 0 failures, late 0, worst 373). Recorded A-128; seed 2 is ready to adopt as the product window RBF. Nothing removed from the card yet.
- 2026-09-20 (Claude): docs. A-128 addendum: the in-menu soak ran at R2; the post-soak playlist check was not run (owed).
- 2026-09-20 (Claude): docs. A-128 closure: post-stress Playlist check PASS 13 (user-reported), so the pump region move is proven.
- 2026-09-20 (Claude): card. Retired the standalone soak (PRB97) and coverage (PRB100) cores after verified backups; kept TAU, TAU_SETTINGS, TAU_DIAGNOSTIC, TAU_SDRAM_WSTRESS. A TAU_PSRAM core (PSRAM session) is on the card, untouched.
- 2026-09-20 (Claude): VM/packaging. B-013: both margin builds (T_ACC 7 and 6, seed 2) Successful, 0 timing failures (setup +0.693/+0.719, hold +0.100/+0.108), RAM 300/308, packing as B-008; RBFs ae01ae5c...6b43 (T7) and e363c5f7...d1a7 (T6); variant bundles `pocket-t7` / `pocket-t6` packaged hash-locked with the X/Y ROM 9e1e65a4...f884b, NOT installed (card not mounted). Card run needs approval.
- 2026-09-20 (Claude): docs/card. A-129: window stress on the seed-2 RBF, track 1 at R1/R2/R3: no failure, L0, M373, S0; PASS for the closing scope. Retired TAU_SDRAM_WSTRESS after a verified backup; cores left: TAU, TAU_SETTINGS, TAU_DIAGNOSTIC (and TAU_PSRAM, not mine).
- 2026-09-21 (Claude): release. A-130: v0.2.0 (release target: settings + Info + SDRAM playlist; APP_VER/core.json/README/CHANGELOG bumped; package.py --rbf option) packaged into dist/ with the probe-free seed-2 RBF (cb15310a...) and release ROM 9b5d6575...; check_tau_package and test-host pass. Not installed on the card.
- 2026-09-21 (Claude): card. Installed release v0.2.0 as the base TAU core (RBF cb15310a..., ROM 9b5d6575..., core.json 0.2.0); old core and settings backed up; 12/13 dist files identical (Platforms/tau.json differs: glyph in card name, untouched); indexes cleared. Result pending.
- 2026-09-21 (Claude): docs. A-131: the user tested release v0.2.0 as the base TAU core (no problems reported); persist file on the card shows non-default volume/colour/meter saved. SDRAM window + playlist work complete; open items listed.
- 2026-09-21 (Claude): firmware/tools. A-132: real Figma meter previews in the meter list (tools/gen_meter_thumbs.py -> fw/meter_thumbs.h, 16-colour palette + RLE, 5,883 B; jpgs were already 56x32) and a ten-step speed list (0.85..2.50, 1.25 replaces 1.2). Settings ROM b80fe44b (heap gap 6,544 B, floor 6 KiB); high speeds expected to underrun. Bundles repackaged, NOT installed; nothing committed.
- 2026-09-21 (Claude): card. Installed the A-132 build (meter previews, ten speeds; ROM b80fe44b...) into TAU_SETTINGS with core.json 0.2.0; previous ROM/core.json/indexes backed up; 14/14 SHA-256. Result pending.
- 2026-09-21 (Claude): docs. A-133: meter previews work (fixed lime colours, not theme-following: open question); speed list works, 1.25x ok and 1.30x clearly degraded (user-reported).
- 2026-09-21 (Claude): firmware. A-134: speed list trimmed to 0.85-1.25x (table keeps all ten), previews unchanged per the user; settings ROM 36d1e37c (gap 6,608 B), diagnostic 62640957; bundles repackaged, NOT installed.
- 2026-09-21 (Claude): card. Installed the A-134 build (speed list 0.85-1.25x, ROM 36d1e37c...) into TAU_SETTINGS; A-132 ROM and indexes backed up; 14/14 SHA-256. Result pending.
- 2026-09-21 (Claude): release. A-135: v0.2.1 (speed list 0.85-1.20x after 1.25x micro-stuttered; meter previews; version/README/CHANGELOG bumped); dist/ rebuilt (ROM 40b92cdf..., RBF unchanged); check_tau_package and test-host pass. Not installed.
- 2026-09-21 (Claude): card. Installed release v0.2.1 as the base TAU (ROM 40b92cdf..., core.json 0.2.1; RBF unchanged) and brought TAU_SETTINGS to the same ROM/version; old files and indexes backed up. Result pending.
- 2026-09-21 (Claude): firmware/tools. A-136: regenerated fw/meter_thumbs.h from the user's grayscale previews (5,747 B, -136 B); settings ROM 69d036da... (gap 6,736 B), TAU_SETTINGS bundle repackaged; dist/ (v0.2.1, lime previews) and the card unchanged. Uncommitted.
- 2026-09-21 (Claude): firmware/release. A-137: meter previews in an 8-level grey palette with 3+5 bit runs (fw/meter_thumbs.h 4,667 B, -1.2 KiB); release v0.2.2 (ROM a8e76b78..., heap gap 7,824 B, RBF unchanged) built into dist/.
- 2026-09-21 (Claude): card. Installed v0.2.2 as the base TAU (ROM a8e76b78..., core.json 0.2.2; RBF unchanged) and TAU_SETTINGS; v0.2.1 files and indexes backed up. Result pending.
- 2026-09-21 (Claude): card. Cleanup: removed TAU_SETTINGS (redundant with the base TAU) and the orphan probe leftovers (19 Settings folders, 3 platform json, 6 platform images) after verified backups (84 MB in work/diagnostics/card-cleanup-2026-09-21). Left: TAU, TAU_DIAGNOSTIC, TAU_PSRAM.
- 2026-09-21 (Claude): card. B-014: installed the two margin cores (`TAU_PSRAM_T7`, `TAU_PSRAM_T6`) additively on `Pock`: bundle hashes checked, 13/13 files per core SHA-256-identical, B-008 core untouched, unmounted. The card had already lost the other Tau cores and the five indexes (not by me; nothing to back up). Run pending (owner, screenshots).
- 2026-09-21 (Claude): docs. Consolidated for handoff: new docs/SESSION_HANDOFF_2026-09-21.md (supersedes the 09-20 one), rewrote docs/CURRENT_STATUS.md, updated PROJECT_REGISTER, SDRAM_MEMORY_ARCHITECTURE status, SETTINGS_* notes and CLAUDE.md session-start section. Pushed.
- 2026-09-21 (Claude): Pocket. B-015: PSRAM read-timing margin measured from 16 screenshots and 2 saved records: index 7 PASS (4 of 4), index 6 FAIL (2 of 2, 22-36% of checks, worst die 78-87%), 8/9/10 PASS; model predicted 7 would fail, hardware says the conservative tAADV reading is pessimistic; default 9 keeps two clocks (33 ns); recommendation keep 9. Timing contract and plan updated. Card read-only, unmounted.
- 2026-09-21 (Claude): docs. Recorded the owner's explanation in B-014: the other Tau cores were removed from the card on purpose (cleanup after releases 2.0-2.2); no action.
- 2026-09-21 (Claude): docs/skill. Updated local KB-037 with the measured read margin (B-015); kb.py validate/index pass.
- 2026-09-21 (Claude): RTL/firmware/tools, no hardware. B-016: PSRAM P4 drafted: uncached CPU window at 0xA400_0000 (decode, mp3_soc bus path, owner mux with the mailbox, guard ACK), firmware L/R modes (window suite, soak, cross-check, cost), PSW1 record and decoder, deferred packing fix; `make test` passes (27), real-CPU sim passes incl. an injected fault; predictions recorded (chain AF0AA680, 1049216 checks, cost ~32/26). Quartus build and card install NOT started: awaiting approval.
- 2026-09-21 (Claude): docs/RTL. B-017: reviewed the completed SDRAM/UI documentation against PSRAM P4 (plan section 8, 11 points): P4 touches shared shipping RTL so the build plan gains a product-config regression build compared bit-for-bit with the shipped RBF (551e5a76...718b) and the Diagnostic Build gates on the P4 RBF; found `tau_psram_bus.sv` missing from `ap_core.qsf` and listed it; A-121 closed; CORE_VERSION deliberately not bumped until P5. Stale statements in the other side's docs listed, not edited. No build launched.
- 2026-09-21 (Claude): VM. B-018: staged and launched (02:12 WEST, after owner approval) the two P4 builds, both seed 2: the PSRAM window diagnostic (TAU_PHASE2_WINDOW + TAU_PSRAM_PROBE + TAU_PSRAM_WINDOW + FAST_*) and the product-configuration regression build (TAU_PHASE2_WINDOW only, compared bit for bit with the shipped RBF 551e5a76...718b). Stages match the working tree except the qsf lines; RTL manifest b21a9c0a.... Result pending.
- 2026-09-21 (Claude): VM. B-018 synthesis passed for both P4 builds; the diagnostic contains the PSRAM window (bus wrapper present, 8,044 registers), the product-config build contains no PSRAM logic (7,470). Fits running; the owner will ask for the check when they finish.
- 2026-09-21 (Claude): docs/tools. B-019: wrote `docs/SESSION_HANDOFF_PSRAM_2026-09-21.md` and `tools/quartus_fit_summary.py`, updated the PSRAM plan/evaluation-plan/roadmap and made small marked corrections in the SDRAM side's `CURRENT_STATUS.md` and `SESSION_HANDOFF_2026-09-21.md` (PSRAM pins, stale margin-build status). Builds B-018 still running at handover; nothing installed; PSRAM RTL/firmware/tools uncommitted.
- 2026-09-21 (Claude): git. B-020: committed the PSRAM work: `25ae8b6` (fw/build.sh: restore the misplaced player-diagnostic label; the committed script had not parsed since v0.2.0 `af28127`, found with `bash -n` per commit) and `82f5f90` (26 PSRAM files; RTL identical to what the B-018 builds were staged from); docs commit follows. Left out: docs/vendor, .claude, work, UniClaudeProxy. Builds B-018 still running.
- 2026-09-21 (Claude): VM/packaging. B-021: read both B-018 P4 builds (seed 2): diagnostic Successful (300/308 RAM, setup +1.505, hold +0.112, dq_out and cram_a now packed); product-config regression build Successful but its RBF is NOT bit-identical to the shipped 551e5a76... (8444e924..., setup +0.638, hold +0.119, +47 registers; shared RTL logic differs even with macros off), so A-114 seed 2 stays the product. Package `pocket-w` (TAU_PSRAM_W, rbf_r verified) built hash-locked, NOT installed; awaiting card approval.
- 2026-09-21 (Claude): card. B-021: installed the P4 diagnostic `TAU_PSRAM_W` additively on `Pock` (index backup verified, 13/13 SHA-256-identical, five indexes cleared); other cores untouched. Result pending.
- 2026-09-21 (Claude): Pocket/docs. B-022: P4 on the Pocket PASS: window suite 1,049,216 checks 0 fail, soak 1000 passes / 1,049,216,000 checks / 0 failures (30+ min), CRCs, chain 0xAF0AA680 and cost RD 32/32 WR 26/26 all as predicted in B-016; decoder PASS. Card only read. KB-040 added (local). Open: SDRAM-unchanged gate (Diagnostic Build on the P4 RBF), then owner decides P5.
- 2026-09-21 (Claude): packaging. B-022: packaged the Diagnostic Build on the P4 RBF (`work/diagnostics/diagnostic-build/pocket`; ROM e4428800... unchanged, rbf_r 407a7bcc...; previous bundle kept as pocket-pre-p4-20260921). NOT installed; card replacement needs approval (back up TAU_DIAGNOSTIC first).
- 2026-09-21 (Claude): card. B-022: installed the Diagnostic Build on the P4 RBF into `TAU_DIAGNOSTIC` (rbf_r 407a7bcc..., ROM e4428800..., core.json 0.2.2; old core/assets/indexes backed up and verified in diagnostic-build/card-replaced-p4; 14/14 SHA-256). Result pending.
- 2026-09-21 (Claude): docs. B-023: SDRAM-unchanged gate on the P4 RBF PASS (Tests 89 / 48-57-335 / 31-38-344 / playlist 13; Stress R1-R3 late 0; 30 min soak 29.1 M ops, 0 failures, late 0, worst 373). P4 exit met. Post-soak playlist check not photographed. Pending: card cleanup/renaming to TAU PSRAM NN (owner to confirm keep-set); P5 decision.
- 2026-09-21 (Claude): card. B-023: removed TAU_PSRAM, TAU_PSRAM_T6 and TAU_PSRAM_T7 after a verified local backup (psram-diag/card-removed-2026-09-21); kept TAU, TAU_DIAGNOSTIC and TAU_PSRAM_W. Renaming not done.
- 2026-09-21 (Claude): firmware/docs. B-024: P5 first move: album-art accumulator (11,040 B) now lives in PSRAM behind `TAU_ART_PSRAM` (new `.psram` section at 0xA4000000, proven at first use, art off on failure); on in release, settings and diagnostic targets so normal and diagnostic builds stay in tandem. Ships on the existing P4 bitstream, no Quartus build needed. Heap gap release 18,736 B, diagnostic 15,104 B. make test-host passes; dist ROM untouched. Not packaged, not on the card.
- 2026-09-21 (Claude): packaging. B-024: packaged `TAU PSRAM 03` (release-style) and `TAU PSRAM 04` (diagnostic) on the P4 RBF; new packager option `--number`. Not installed; install will also remove TAU_DIAGNOSTIC after backup.
- 2026-09-21 (Claude): card. B-024: installed TAU PSRAM 03 and 04 with media copies (verified), removed TAU_DIAGNOSTIC after backup. Cores left: TAU, TAU_PSRAM_W, 03, 04. Result pending.
- 2026-09-21 (Claude): tools/firmware. B-025: `tools/sync_media.py` (copy/convert/verify media into chosen Tau cores; use on every new build) and Diagnostics > ALL SPEEDS in the Diagnostic Build; new diagnostic packaged as TAU PSRAM 05 (03 and 04 stay as installed). Not installed, nothing committed.
- 2026-09-21 (Claude): card/tools. B-025: sync_media.py now embeds folder covers into the copies of tracks (player only reads embedded art) and generates missing playlists; installed TAU PSRAM 05, removed 04; new albums (455/1400/1499 px covers) copied to 03 and 05 and verified (68 hashes). Test script in docs/TEST_SCRIPT_B024.md. Result pending.
- 2026-09-21 (Claude): docs/tools/card. B-026: read the 05 results (Tests PASS 89, soak PASS 31.3 M ops, late 0, covers incl. FLAC show); two accented-name tracks were skipped (BUG-001, still open in firmware); sync_media.py now writes ASCII-only names and rewrites playlists; 03 and 05 media re-synced and re-verified. Art decode timing not yet measured (no Info screenshots).
- 2026-09-21 (Claude): docs/firmware. B-027: 03's Info pages show art decode 5.3 s (455 px) and 15.8 s (1400 px) vs ~1.1 s expected in BRAM: the PSRAM accumulator is about 4x slower than predicted (prediction 0.3-0.6 s was wrong). Built the A/B control TAU PSRAM 06 (same RBF, art in BRAM; `ART_PSRAM=0` build.sh override), not installed. Release on hold until the comparison.
- 2026-09-21 (Claude): tools/docs. B-027 addendum: the Figma covers are unoptimized (455 px = 255 KB, 9.9 bpp; the A-120 reference cover was 36 KB), so decode time is dominated by file weight and the earlier 1.1 s BRAM estimate was invalid; sync_media.py gained --cover-quality/--cover-max/--dest-suffix and now removes non-ASCII characters with no plain equivalent. A/B matrix (03 vs 06 x heavy vs q75 covers) proposed, not run.
- 2026-09-21 (Claude): firmware/docs. B-028: per owner, the PSRAM art A/B is parked until the blit engine exists; build.sh now defaults art in BRAM for release and settings builds (gap 7,824 B), the diagnostic build keeps PSRAM art (gap would be 3,584 B otherwise). Nothing installed or committed.
- 2026-09-21 (Claude): release. B-029: v0.3.0 built and zipped as release/alfatreze.TAU_0.3.0_2026-09-21.zip (Analogue naming Author.Core_Version_Date.zip): P4 bitstream with PSRAM, art buffer in PSRAM, new platform artwork (convert_pocket_art.py now runs without Pillow), CHANGELOG/README/APP_VER/core.json bumped; package checks and host tests pass. Not installed or tested on the Pocket, not committed.
- 2026-09-21 (Claude): release. B-029 addendum: `tools/make_release.py` builds and zips BOTH cores per release (alfatreze.TAU and alfatreze.TAU_DIAGNOSTIC, Analogue naming, checks, SHA256SUMS); v0.3.0 zips produced; README gained a Diagnostics section (what it is for, how to send results) and corrected controls/speed text. Not installed/tested on the Pocket, not committed.
- 2026-09-21 (Claude): card. B-029: installed the v0.3.0 release as TAU and the Diagnostic Build as TAU_DIAGNOSTIC (+media), removed test cores 03/05/W after verified backup; cores on the card: TAU, TAU_DIAGNOSTIC. Smoke test pending.
- 2026-09-21 (Claude): git/docs. B-030: committed v0.3.0 locally in four commits and tagged `v0.3.0` (not pushed; owner skipped the release smoke test); wrote `docs/SESSION_HANDOFF_2026-09-21_RELEASE_0.3.md`, `docs/MEDIA_LIBRARY_0.4_BRIEF.md` and `docs/MEDIA_LIBRARY_0.4_PROMPT.md`; updated CURRENT_STATUS and the session-start pointer; KB-042 added. Next: 0.4 media library design.
- 2026-09-21 (Claude): git. Pushed main (665895c..9104a06, eight commits incl. the PSRAM work) and the tag v0.3.0 to origin (alfatreze/Tau-Alpha) at the owner's request; upstream untouched. Release zips are not attached anywhere (release/ is local).
- 2026-09-21 (Claude): release. Published GitHub release v0.3.0 (https://github.com/alfatreze/Tau-Alpha/releases/tag/v0.3.0) with both zips and SHA256SUMS.txt at the owner's request; owner reports a quick test of both cores worked. `tools/make_release.py` output can be attached the same way (`gh release create vX.Y.Z release/*.zip release/SHA256SUMS.txt`).
- 2026-09-21 (Claude): docs only. B-031: wrote `docs/MEDIA_LIBRARY_0.4_SPEC.md` (index/thumbnail formats, size budget, PSRAM map, latency targets, failure codes, tool CLI, firmware plan, tests, Pocket plan with predictions). Found the absolute-path open is untested on hardware and that `TAU_LIBRARY` needs art in PSRAM. No code, card or VM touched.
- 2026-09-21 (Claude): tools/tests. B-032: media library phase 1: `tools/tau_library.py` (index builder/validator/reference loader/synthetic), `sync_media.py --library`, `sim/test_library_index.py` in make test-host (passes). Host only; no card, VM or firmware touched.
- 2026-09-21 (Claude): tooling. JTAG on the Mac: Terasic USB-Blaster works through UTM USB passthrough into the Quartus VM (VM needs /etc/udev/rules.d/51-usbblaster.rules, MODE=0666 for 09fb). jtagconfig sees the Pocket FPGA (5CEBA4, ID 02B050DD). Loaded the B-018 P4 diagnostic ap_core.sof with quartus_pgm (about 5 s, succeeded) and the Pocket reloaded the core (owner-reported). No RTL, card or repo files changed. Next options: SignalTap on the next diagnostic build; JTAG-to-Avalon master for memory/frame reads.
- 2026-09-21 (Claude): docs only. Wrote `docs/JTAG_DEBUG_ACCESS.md` (Mac + UTM + VM procedure, what JTAG can/cannot give) and added roadmap Phase F0 (JTAG debug access, immediately before the blit engine) plus a handoff open item. Not committed; no code, card or VM change.
- 2026-09-21 (Claude): docs only. Read the B-018 P4 diagnostic fit (M10K 300/308 so 8 free, MLAB 0 used, ALMs 6,459/18,480, setup +1.505 ns) and added a SignalTap plan (MLAB buffers, three instances, macro TAU_SIGNALTAP, first a tiny proof build) to docs/JTAG_DEBUG_ACCESS.md section 5. Nothing built; no code, card or VM change.
- 2026-09-21 (Claude): RTL/tools/docs, no build. Prepared the SignalTap proof build: `tau_signaltap_tap.sv` + a 12-line `ifdef TAU_SIGNALTAP` instance in mp3_soc.v (compiled out by default), `tools/gen_signaltap_stp.py`, `tools/signaltap_proof_qsf_append.txt`, docs/JTAG_DEBUG_ACCESS.md s5.5 and roadmap Phase F0 (before the blit engine). The .stp format is unverified; first check is a synthesis-only run on the VM. Nothing launched or committed; mp3_soc.v is shared RTL (default builds unaffected).
- 2026-09-21 (Claude): docs/staging. B-033: JTAG/screen docs re-checked (0 drift); staged the library phase 0 absolute-path playlist test (`docs/LIBRARY_PHASE0_TEST.md`, work/diagnostics/library-phase0/stage). Card only read, not written; awaiting approval.
- 2026-09-21 (Claude): docs only. B-034: roadmap tooling track (firmware harness at Phase E, card-push script before first library install, JTAG RAM loader as a Phase F gate). Nothing else touched.
- 2026-09-21 (Claude): card. B-033: installed Phase0/phase0.m3u into TAU_DIAGNOSTIC media (additive, verified, ejected). Result pending (owner run).
- 2026-09-21 (Claude): VM/docs. Staged the SignalTap proof build (`/home/taualpha/tau-local/signaltap-proof-20260921`) and ran synthesis only: Successful, 0 errors, 5m22s; the 32 tap registers are in the netlist. The generated .stp is rejected by quartus_stp (Internal Error ice_instance_builder.cpp:685, same for 9 variants); no reference .stp or GUI on the VM. Needs one GUI-made reference file (docs/JTAG_DEBUG_ACCESS.md s5.6). No fit/full build run; nothing installed or committed.
- 2026-09-21 (Claude): docs. B-033: read the card after the owner's run: one screenshot (playlist loaded with all 7 entries); open/playback outcome per line still needed. Card only read.
- 2026-09-21 (Claude): docs/KB. B-033 result: absolute-path open PASS on hardware (lines 2-6 play, missing line skipped); relative control line 1 was skipped (probable slot-directory join, unverified). KB-043 added. Spec updated; library design unchanged.
- 2026-09-21 (Claude): docs. B-035: BUG-002 (now-playing title clipped at about 45 characters, buffer track_title[48]) written up as docs/issues/020; fix planned with phase 2 firmware. No firmware changed.
- 2026-09-21 (Claude): firmware(host)/tests. B-036: library phase 2 start: portable `fw/library_core.h` + rv32sim harness + `sim/test_library_fw.py` (firmware equals Python reference on readbacks and 13 corruption cases; in make test-host). player.c untouched; no card/VM.
- 2026-09-21 (Claude): firmware/tools. B-037: BUG-002 fixed (title buffers 80/64); library player integration behind TAU_LIBRARY (fw/library.inc, player-library target, ROM 171,180 B, gap 8,320 B); 5 library UI fixtures; packaged TAU PSRAM 07 (not installed). make test-host passes. Accidental git stash restored (no loss). Card/VM untouched.
- 2026-09-21 (Claude): card. B-037: installed TAU PSRAM 07 + test media + library index (30 tracks, verified, ejected); fixed tool index root to follow the platform folder. Cores on card: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_07. Result pending (owner run).
- 2026-09-21 (Claude): firmware. B-038: owner reported 07 works; fixed scroll-bar gap (rows stop short of the bar), Select from Settings opens the list, FLAC end-of-list LOAD FAILED (soft restart used the MP3 decoder; now stop_req/flac_restart; failure screen also returns on a playlist pick; list_ended knows the library). Packaged TAU PSRAM 08, not installed. Card untouched.
- 2026-09-21 (Claude): firmware/tools. B-039: MENU/SETTINGS restructure (Info + How it works; Diagnostics inside Settings in the Diagnostic Build), legacy playlist mode alert + help text, legacy paths ignored when a library is loaded, library playlists (core, tool policy, UI, harness/tests, fixtures). Packaged TAU PSRAM 09 (not installed). Card/VM untouched.
- 2026-09-21 (Claude): VM/tools/docs. Made a GUI reference .stp with the owner (gdm3 in the VM), regenerated `tools/gen_signaltap_stp.py` from it (quartus_stp accepts it) and re-ran synthesis: Successful, SignalTap inserted (32 taps). The MLAB request is resolved to M10K (+65,536 bits, about 7 of 8 free blocks); MLAB spelling or a 512-sample buffer needed before a fit. quartus_stp --enable caches the stp in the project. No fit, nothing installed or committed. See docs/JTAG_DEBUG_ACCESS.md s5.7.
- 2026-09-21 (Claude): card. B-039: installed TAU PSRAM 09 + media + Favourites.m3u + index (30 tracks, 2 playlists), removed 07 after backup, ejected. Result pending (owner run).
- 2026-09-21 (Claude): docs only. B-040: parked the Settings enable/disable-library switch (spec section 13, roadmap Phase E). Nothing else touched.
- 2026-09-21 (Claude): firmware/tools. B-041: library history (persist words 12-15) and load-not-play boot, info-first frame, x/y list counter, Settings-style library lists, MENU>SETTINGS>LIBRARY on/off page; packaged TAU PSRAM 10 (not installed). Card/VM untouched.
- 2026-09-21 (Claude): card. B-041: counter now reuses the existing transport-row x / y (also for the library queue); rebuilt, packaged and installed TAU PSRAM 11 (replaces 10, 09 removed after backups), media + index re-synced, ejected. Result pending.
- 2026-09-21 (Claude): firmware. B-042: splash shows only the loading bar; player-screen loading state (full frame, animated loader + 'Loading track' centred in the art plate); Pocket colour palette (19 colours, default WHITE, interact Color max 18/default 1); built, packaged and installed TAU PSRAM 12 (11 removed after backup). Result pending.
- 2026-09-21 (Claude): docs only. B-043: parked the remaining library items until CPU RAM is freed (spec section 14, roadmap Phase E note); memory options assessed. Nothing else touched.
- 2026-09-21 (Claude): docs only. B-044: wrote the Tau Browser spec set and Codex prompt in ../Tau Browser/ (Tauri 2 + Rust + TypeScript recommended). Nothing in this repo changed apart from the log.
- 2026-09-21 (Claude): docs only. B-045: wrote docs/PHASE_G_SPEC.md (cold code/data out of on-chip RAM; options, recommendation C then B, phases G0-G5, decisions). Nothing built or changed.
- 2026-09-21 (Claude): firmware/tools. B-046: Phase G1: cold data image (tau-cold.bin, data slot 6, layout-bound to the ROM, host-tested loader, previews + help text moved), heap gap 6,976 -> 10,960 B; packaged TAU PSRAM 13 (not installed). Card/VM untouched.
- 2026-09-21 (Claude): RTL/sim. B-047: Phase G2: PSRAM instruction fetch (alias 0x2400_0000, second read-only bus, two-client arbiter, counters 0xB0-0xB8, parameter default off); real-CPU simulation passes, make test-rtl and test-host pass. No Quartus, nothing installed. mp3_soc.v also holds another session's SignalTap edits.
- 2026-09-21 (Claude): RTL plumbing. B-047 addendum: TAU_PSRAM_IFETCH macro in core_game.vh + qsf append file; G3 build plan (seeds 1,2, no SDRAM counter) waiting for VM approval.
- 2026-09-21 (Claude): VM. B-048: launched the two G3 builds (PSRAM window + IFETCH, seeds 1 and 2, 19:12 and 19:14 WEST) after owner approval; stage proven identical to the tree except the qsf. Result pending.
- 2026-09-21 (Claude): firmware. B-049: Phase G cold-code firmware: image = data+padding+code, .cold_text linker section, boot checks (bitstream feature bit, probe call), Tests > COLD CODE TEST, player-cold-diagnostic target, packager --cold; TAU PSRAM 14 packaged on the old bitstream for the fail-safe test (not installed). G3 builds still running.
- 2026-09-21 (Claude): card. B-049: installed TAU PSRAM 14 (cold-code diagnostic, old bitstream; fail-safe test) additively, ejected; fixed the boot loading frame for library tracks (source only). G3 builds still running.
- 2026-09-21 (Claude): docs/KB. B-049 result: cold image loads and verifies on the Pocket (12040 B, 33 ms); cold code correctly refused on the old bitstream (E18), no crash; Info row now shows the E-code. KB-044 added. G3 builds still running.
- 2026-09-21 (Claude): VM/packaging. B-050: G3 builds done (0 errors, hold +0.132/+0.122, setup +0.799/+1.077); seed 1 chosen by the rule; TAU PSRAM 15 (cold-code diagnostic on the new bitstream) packaged, not installed; awaiting card approval.
- 2026-09-21 (Claude): card. B-050: installed TAU PSRAM 15 (cold-code diagnostic on the G3 bitstream), removed 14 after backup, ejected. Result pending (first hardware run of PSRAM instruction fetch).
- 2026-09-21 (Claude): firmware/docs. B-051: owner notes on core 15: fixed the loader/caption left over the cover (cancel after final chrome, two-line caption); boot slowness and Settings glitch need numbers/screenshots; explained soak and repeat runs. Not installed.
- 2026-09-21 (Claude): firmware/packaging. B-052: core 15 was the old Diagnostic Build line without the library (my omission); added player-library-diagnostic (combined, gap 5,008 B) and packaged TAU PSRAM 16 on the G3 bitstream; not installed.
- 2026-09-21 (Claude): docs only. B-053: wrote docs/TEST_SUITE_SPEC.md (on-device automated test suite for the Diagnostic Build: profiles, catalogue, result channels, phases). Nothing built.
- 2026-09-21 (Claude): docs. B-053 addendum: diagnostics redesigned for users (USER CHECK in the release, report code + one screenshot + optional file, Tau Browser Send diagnostics). Not built.
- 2026-09-21 (Claude): docs. B-053 addendum 2: report as a QR code (TAUD1 payload, pages, short-code fallback) specified. Nothing built.
- 2026-09-21 (Claude): docs. B-054: TAU PSRAM 15 on the Pocket PASS (cold code from PSRAM 31.6 C/W as predicted, window 89, SDRAM gate within 1 cycle, 30-min soak 29.8 M ops 0 failures late 0); worst access 380 vs 373 noted; boot length is the 15 s PSRAM art decode, not the cold image. No code, card or VM touched.
- 2026-09-21 (Claude): firmware/tests. B-055: fw/qrcode.h QR encoder verified against segno for versions 1-25 and decoded with OpenCV (make test-qr); fixed a v11 scratch-size bug. Not in any build yet.
- 2026-09-21 (Claude): firmware/tools. B-056: diagnostics S1: TAUD1 report record + decoder (`tools/decode_tau_suite.py`, `sim/test_suite.py` in test-host), Settings > Check runner (`fw/suite.inc`, TAU_CHECK, all cold code, fixed-mask QR), persisted summary in words 8-11; TAU PSRAM 16 repackaged (ROM 20f8c34c..., heap gap 4,144 B), NOT installed, not run on a Pocket.
- 2026-09-21 (Claude): card. B-057: installed TAU PSRAM 16 (library + Diagnostic Build + cold code + Settings > Check; 15/15 SHA-256, media + library index from core 15), removed 15 after backup; cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_PSRAM_16. Result pending (first Check run).
- 2026-09-21 (Claude): docs/firmware. B-058: first Check run on TAU PSRAM 16 read (screen, QR and persist file agree); SDRAM window never proven in library mode (bug) fixed, Y reruns directly, Check moved into Diagnostics (diagnostic build); TAU PSRAM 17 packaged, NOT installed.
- 2026-09-21 (Claude): card. B-059: installed TAU PSRAM 17 (15/15 SHA-256, media + index from 16), removed 16 after backup; cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_PSRAM_17. Result pending.
- 2026-09-21 (Claude): docs/firmware. B-060: core 17 run read (SDRAM window now PASS; SDRAM speed failed because the rule judged averages under load); rule now uses the best of 256, record carries min/avg/max; TAU PSRAM 18 packaged, NOT installed. B confirmed to stop a run.
- 2026-09-21 (Claude): tools. Test builds are now named TAU_DEV_NN (packager --number); TAU DEV 18 packaged (not installed), numbering continues.
- 2026-09-21 (Claude): card. B-061: installed TAU DEV 18 (15/15 SHA-256, media + index from 17; slow because the card was mounted over the Pocket USB), removed 17 after backup; cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_DEV_18. Result pending.
- 2026-09-21 (Claude): docs. B-062: TAU DEV 18 first run: all seven Check lines PASS (SDRAM best 46/31, PSRAM 39/30), screen, QR and persist file agree; PSRAM rule widened in source (next build); S1 exit met for the Diagnostic build.
- 2026-09-21 (Claude): firmware/tools/docs. B-063: Check developer profiles (STANDARD/FULL with stress and soak, Diagnostic Build), release-style Check build, four Check fixtures, README Check section, Tau Browser Send-diagnostics spec; TAU DEV 19 (diagnostic) and TAU DEV 20 (release-style) packaged, NOT installed.
- 2026-09-21 (Claude): card. B-064: installed TAU DEV 19 (diagnostic, profiles) and TAU DEV 20 (release-style Check), 15/15 SHA-256 each, media from 18; removed 18 after backup. Cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_DEV_19, TAU_DEV_20. Result pending.
- 2026-09-21 (Claude): firmware/docs. B-065: Check kept Diagnostic-Build-only (owner); fixed Left/Right in settings pages seeking the track (keys not masked; also affects release Volume); TAU DEV 21 packaged, NOT installed; DEV 20 to be removed on the next card write.
- 2026-09-21 (Claude): firmware/docs. B-066: Check ENDURANCE profile, soak length/level options, track-change and cold x20 tests (Diagnostic Build); heap gap at the 4,096 B floor; TAU DEV 22 packaged, NOT installed (supersedes unreleased 21).
- 2026-09-21 (Claude): docs/firmware. B-067: TAU DEV 19 results read (USER CHECK, stress profile PASS; FULL soak PASS, playback 1 late underrun); fixed Start closing the result pages, QR header profile, per-second full redraw; TAU DEV 23 packaged, NOT installed.
- 2026-09-22 (Claude): card. B-068: installed TAU DEV 23 (15/15 SHA-256, media from 19), removed 19 and 20 after backup; cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_DEV_23. Result pending.
- 2026-09-22 (Claude): firmware. B-069: Phase G4 step 1: library and settings UI now cold code (TAU_G4): heap gap 4,096 -> 21,584 B (diagnostic), 11,424 -> 25,920 B (release-style); tools/check_cold_calls.py; TAU DEV 24 (G3 bitstream) and 25 (old bitstream, fail-safe test) packaged, NOT installed.
- 2026-09-22 (Claude): docs/firmware. B-070: DEV 23 FULL x2 all PASS except track changes SKIPPED (test now reports why); G4 steps 2-3 (playlist loader/overlay, cover-art glue cold): heap gap 30,016 B (diagnostic) / 34,240 B (release-style); TAU DEV 28 (G3) and 29 (old bitstream, fail-safe) packaged, NOT installed.
- 2026-09-22 (Claude): card/docs. B-071: installed TAU DEV 28 (G3) and 29 (old bitstream, fail-safe), 15/15 SHA-256 each, removed 23 after backup; wrote docs/TEST_SCRIPT_B071.md (full user test suite for G4). Result pending.
- 2026-09-22 (Claude): firmware/docs. B-072: Left/Right are Back/Forward in menus, library and playlist overlay (L1/R1 now page the playlist); README and test script updated; TAU DEV 30 packaged, NOT installed.
- 2026-09-22 (Claude): firmware/docs. B-073: fixed library-mode track skip (pl_count gate) and made menu scrolling auto-repeat; investigated the small-cover-not-loading bug (parsing logic proven correct on the host; un-cold art_find_apic as an experiment, added Info > COVER diagnostics); corrected Check time estimates in the test script; TAU DEV 31 packaged, NOT installed.
- 2026-09-22 (Claude): firmware. B-073 addendum: legacy playlist rows restyled to match settings/library (36px, 7 rows, PLIST_ROW_H/PLIST_ROWS); other pages unaffected; TAU DEV 32 packaged, NOT installed.
- 2026-09-22 (Claude): card. B-074: installed TAU DEV 32 (15/15 SHA-256, media from 28), removed 28 after backup; kept 29 (fail-safe test). Cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_DEV_29, TAU_DEV_32. Result pending.
- 2026-09-22 (Claude): firmware/docs. B-075: found the real cause of the disappearing cover art (the library loader wiped the off-screen art stash on every track, defeating the same-picture reuse path); fixed by not wiping it there; art_find_apic restored to cold code. TAU DEV 33 packaged, NOT installed.
- 2026-09-22 (Claude): card. B-076: installed TAU DEV 33 (15/15 SHA-256, media from 32), removed 32 after backup; kept 29. Cores: TAU, TAU_DIAGNOSTIC, TAU_PSRAM_12, TAU_DEV_29, TAU_DEV_33. Result pending.
- 2026-09-22 (Claude): docs. B-077: cover fix confirmed on TAU DEV 33; TAU DEV 29 fail-safe test passed in full (J1-J8). TAU_PSRAM_12 flagged stale for removal on the next card write.
- 2026-09-22 (Claude): release. B-078: v0.4.0 release candidate built (media library, Phase G/cold code, all fixes since 0.3.0 in the actual shipped release, not just test builds); new tools/tau_data_slots.py shared slot declarations; package.py --release-library; make_release.py builds player-library-diagnostic for the Diagnostic Build; CHANGELOG/README updated. Both zips built and checked, make test passes. Not installed, not committed, not tagged.
- 2026-09-22 (Claude): card/release. B-079: installed v0.4.0 as the base TAU and Diagnostic Build (media + library index synced from TAU DEV 33), removed TAU_DEV_29, TAU_DEV_33 and TAU_PSRAM_12 after backup. Cores: TAU, TAU_DIAGNOSTIC (both 0.4.0). Smoke test pending.
- 2026-09-22 (Claude): firmware. B-080: fixed the blank/UNKNOWN TRACK screen after closing the library with nothing loaded (ui_draw_chrome now handles the empty state, replacing the separate boot idle card); added an Info R0/R1 marker to gather evidence on the TAU-vs-Diagnostic boot-restore mismatch; the missing loading-message-on-album-pick bug not reproduced from source, left open. v0.4.0 zips rebuilt; TAU DEV 34 packaged, NOT installed.
- 2026-09-22 (Claude): card. B-081: installed the B-080 firmware fixes onto TAU and TAU_DIAGNOSTIC (ROM/cold-image swap only, media and settings untouched), added TAU_DEV_34 for separate testing. Result pending.
- 2026-09-22 (Claude): docs. Wrote docs/SESSION_HANDOFF_2026-09-22_RELEASE_0.4.md (full project knowledge: v0.4.0 state, firmware map, two bug post-mortems, decisions, procedures, open items) and rewrote docs/CURRENT_STATUS.md; CLAUDE.md session-start pointer updated. Nothing else changed.
- 2026-09-22 (Claude): docs only. B-082: owner parked the two remaining B-080 items (release-vs-diagnostic boot-restore mismatch, missing loading-message on an album pick) as UX friction, not blocking; updated AUDIT_TRAIL, the 0.4 handoff and CURRENT_STATUS to reflect this and re-numbered the open-items list. Instrumentation (Info R0/R1) left in place. No code, card or VM touched.
- 2026-09-22 (Claude): docs only. B-083: owner confirmed the actual next RTL build is the Phase F blit engine (JTAG/SignalTap's proof was synthesis-only, no Quartus slot spent); re-targeted the SDRAM busy-cycle counter (previously "next RTL build", deliberately skipped in every build since Phase B was written so timing stayed attributable) at that build in ARCHITECTURE_ROADMAP.md and the 0.4 handoff's open-items list. No code, card or VM touched.
- 2026-09-22 (Claude): docs only. B-084: wrote `docs/PHASE_F_SPEC.md` (blit engine feature tiers, the M10K map + release ledger, prior-art/licensing review, MMIO descriptor decision, build/verification/fail-safe plan, parked 2D ideas) and updated ARCHITECTURE_ROADMAP (budget table + MLAB row, Phase B item 1 done, Phase D corrections, Phase F rewrite, no-3D decision, Phase G to 192 KB, licence + MMIO decisions) and EQ_DESIGN status. Key results: main RAM is 256 of 300 M10K; MLAB is entirely unused (~7 blocks recoverable); the RAM shrink is gated on the blit engine, not the reverse; no FFT (hardware octave bank instead); 3D dropped; kernel ordering corrected to the measured FLAC bit reader. No code, card or VM touched. **Correction appended the same day:** the entry's claim that the repo had no LICENSE file was wrong (shell-glob artifact) — `LICENSE` is MIT and tracked since the first commit, upstream HarpMudd was already MIT at the fork point, and the real constraint is that Tau being MIT means GPL RTL cannot be copied in. See B-084.
- 2026-09-22 (Claude): docs only. B-085: completed the Phase F plan — licence decision (keep MIT; corrects B-084's wrong "no LICENSE file" claim, which was a shell-glob artifact — the real consequence is that MIT *forbids* copying GPL RTL in), MMIO descriptor model (split sticky engine state from per-command FIFO fields; 3 registers, not one per parameter, and don't widen cmd_mem), RAM-shrink gate (stack painting for peak usage, worst-case profile, 16 KB margin written down first), the blit-storm Check test that publishes the SDRAM busy percentage alongside the L0 verdict, and a synthesis-first build strategy with per-piece macros. Next item recorded as the never-run decoder profile. CURRENT_STATUS and the session-start pointer now lead to `docs/PHASE_F_SPEC.md`. No code, card or VM touched.
- 2026-09-22 (Claude): firmware/tools/docs. B-086: Phase D step 1 instrumentation built (per-stage MP3/FLAC decode cost) -- reused fw/flac.c's dormant FLAC_PROFILE tick-hook mechanism and mp3dec.c's own dead stock PROFILE scaffolding rather than inventing a new one; new MP3_PROFILE hook (H=Huffman/bit-read, I=IMDCT, S=Subband), FLAC_PROFILE wired for the first time, bench-only UI_SHOW_DECODE_PROFILE row, `fw/build.sh player-profile` target. Verified byte-identical on the normal `player` build (dead-code-eliminated) and make test-host passes. Predictions recorded in ARCHITECTURE_ROADMAP.md section 2 before any hardware run. NOT built for hardware, NOT packaged, NOT installed -- awaiting approval per the task's own instruction.
- 2026-09-22 (Claude): tools/card. B-086 addendum: extended tools/package_sdram_stress.py with --profile (reuses the current bitstream unchanged, no RTL); packaged TAU DEV 35. Card: backed up and removed TAU_DEV_34 (verified SHA-256, purpose closed per B-082), installed TAU_DEV_35 (verified SHA-256), synced media (base TAU library + flac-tests vectors + a new mp3-profile/ bitrate spread from work/test-music). Correction: the original FLAC.md test files (Pink Floyd, Furs, Garcia, Circles) are gone (not in-repo, not on the card) -- the refresh will use flac-tests instead, flagged in AUDIT_TRAIL. Five system caches cleared, ejected. NOT run yet -- awaiting the owner to boot it and read the H/I/S/R row.
- 2026-09-22 (Claude): firmware/card. B-086 addendum 3: moved the UI_SHOW_DECODE_PROFILE row from y296 (was covering UI_TIME_Y=288, the elapsed/total clock) to y0..15, the empty strip above the card panel -- owner feedback after the first run. Rebuilt (player build stays byte-identical), repackaged as TAU DEV 36, installed on the card carrying forward the corrected playlist (no re-clone). NOT yet run with the new layout.
- 2026-09-22 (Claude): docs only. B-087: read 11 screenshots from TAU_DEV_36. MP3 measured -- Subband/filterbank dominates (55-59%), Huffman/bit-reading smallest (3-5%), opposite of B-086's prediction; 1.2x-speed retest confirmed no effect (resampling is downstream of Subband). FLAC's R could not be refreshed: flac-tests/t1-t6 are VERBATIM-only test vectors (tools/flac_make_test.py), so R0 is correct, not a bug -- needs a real-music FLAC file. Corrected D3 (PHASE_F_SPEC.md): the pre-D3 roadmap order (filterbank first) holds for MP3 after all; FLAC's bit-reader-first case is separate and untouched. ARCHITECTURE_ROADMAP.md section 2 updated with measured results. No code, card or VM touched (screenshots read-only, card ejected).
- 2026-09-22 (Claude): docs only. B-088: specced folding B-086/B-087's decoder stage cost into the existing Check/QR record (new SR_T_DECPROF tag, run-total percentages not per-second, two independent accumulator sets so the screen row and the QR report share no state, opt-in Diagnostic Build variant). Not built -- scoped as its own follow-up so it doesn't further entangle fw/player.c/fw/build.sh. docs/TEST_SUITE_SPEC.md section 11.
- 2026-09-22 (Claude): card. B-087 addendum: synced the 2 Hyperion FLAC test tracks (real music, not VERBATIM) onto TAU_DEV_36 as Hyperion/, appended to the root playlist (now 11 tracks total). Result pending -- this closes the still-open FLAC R refresh from B-087.
- 2026-09-22 (Claude): firmware/tools. B-089: built B-088's spec -- SR_T_DECPROF tag, two independent accumulator sets (MPROF_ADD/PROF_ADD now advance both from one tick() read), CT_AUD window as the measurement period, new player-library-diagnostic-profile build target, decode_tau_suite.py + sim/test_suite.py updated. make test-host passes (16/16), firmware builds clean. NOT run on hardware. Most of this sits in files already untracked before this session (the whole Check/QR feature) -- left entirely uncommitted, flagged for the owner to decide commit scope.
- 2026-09-22 (Claude): tools/card. B-089 addendum: added package_sdram_stress.py --diagnostic-profile; packaged and installed TAU DEV 37 (removed TAU_DEV_36 after backup), synced library index (35 tracks) covering mp3-profile/flac-tests/Hyperion test folders. NOT run yet -- awaiting the owner to play a track and run Check.
- 2026-09-22 (Claude): docs only. B-090: specced a "Decode Profile Sweep" (batch decode-cost measurement over a library queue, one QR for the whole run) after the owner correctly flagged B-089 as just a screenshot-to-QR swap, not the batch feature asked for. Three design decisions confirmed with the owner (15s/track capped by length, separate Diagnostics menu entry not a Check mode, hard 48-track cap refused rather than paged). docs/TEST_SUITE_SPEC.md section 12. Not built.
- 2026-09-22 (Claude): firmware/tools. B-091: built B-090's Decode Profile Sweep -- new SR_T_DECSWEEP tag (repeatable, 9B/track, NOT a reuse of SR_T_DECPROF -- found during implementation that length-sniffing one tag into two shapes would recreate the shared-state coupling this thread has avoided all along), sw_* state machine in suite.inc mirroring CT_AUD's wait/window/sample pattern but looping over the library queue (lib_qn/lib_qpos/lib_skip), new Settings > Diagnostics > DECODE SWEEP page, decode_tau_suite.py decodes it as a list. make test-host passes (17/17), player-library-diagnostic-profile builds clean, player/player-library-diagnostic confirmed byte-identical (unaffected). NOT run on hardware.
- 2026-09-22 (Claude): firmware/tools/test-assets. B-092: added speed_pct to SR_T_DECSWEEP (10 bytes now, from the existing speed_num/speed_den rational, no new setting or outer loop); built a fixed "Test Album" (2 Hyperion pieces x FLAC/MP3 at 44.1k+128/320 plus a resampled 48k variant, + 1 LibriVox speech clip -- Mark Twain, public domain) replacing the ad hoc mp3-profile/flac-tests sets as the standing regression asset for Phase F. make test-host passes, firmware confirmed unaffected elsewhere. Card install staged, not yet synced.
- 2026-09-22 (Claude): card. B-092 addendum: packaged and installed TAU DEV 39 (removed TAU_DEV_38 after backup), synced the Test Album + base library (41 tracks). NOT run yet -- awaiting the owner to run Decode Profile Sweep over the Test Album.
- 2026-09-22 (Claude): docs only. Handoff prep for a fresh session: rewrote `docs/CURRENT_STATUS.md`'s "next item" (was still pointing at the now-done decoder profile) and `docs/PHASE_F_SPEC.md` section 14's order table (items 1-2 marked done, item 3 -- the blit engine synthesis-only proof -- is next and unblocked) so a fresh session reads the correct next step per this file's own section 2b convention. Confirmed B-086..B-099 are all committed (`422e0a9`, local `main` 26 commits ahead of `origin/main`, not pushed). No code, card or VM touched.
- 2026-09-22 (Claude): RTL/tools, no hardware. B-100: Phase F step 1 (PHASE_F_SPEC.md section 10) -- the two functionally-inert MLAB/font-repack pieces, gated by new macros. `TAU_MLAB_MIGRATE`: forces `ramstyle="MLAB, no_rw_check"` on `glyphbuf` (mp3_fb.sv -- Quartus was not inferring MLAB despite the code comment hoping for it) and an `lpm_hint="RAM_BLOCK_TYPE=MLAB"` on the sound_i2s dcfifo megafunction (sync_fifo.v); `cmd_mem` (riskiest, needs its own timing check per spec) and the VexRiscv regfile (needs hand-patching a generated netlist) deliberately deferred, not "the three easy ones" as originally scoped. `TAU_FONT_REPACK`: reworked tools/gen_font_rom.py into a reusable `render_verilog(words)` and regenerated font_rom.v as a dual-path module (four byte-wide arrays under the macro, the original single 32-bit array otherwise); content-preserving by construction -- parsed the shipped ROM's words directly (no PIL, no re-render) and verified byte-for-byte the repacked lanes reassemble identically. New `tools/blit_step1_qsf_append.txt`. Verified: `iverilog` syntax-clean across all four macro combinations, `make test-host` 0 failures, `make test-rtl` exit 0 (5/5 groups PASSED). Quartus synthesis-only run (`quartus_map ap_core` on the VM, ~5 min, no fit) NOT launched -- this session has no VM/SSH access; exact staging command handed to the owner.
- 2026-09-22 (Claude): VM/docs. B-100 addendum: the "no VM/SSH access" line above was wrong -- a working keypair from an earlier session (`~/.ssh/taualpha_vm_ed25519`) is still authorized on the VM; found this only because the owner asked how to grant access. Staged the tree via `git archive HEAD` over SSH (the usual shared-mount folder was empty), ran `quartus_map ap_core` both with and without the two macros for a clean baseline diff. **Result, and a correction to the spec's own framing:** MLAB migration is confirmed and de-risked (`glyphbuf` + dcfifo both resolved to `MLAB` in the RAM Summary, a clean 2,176-bit 1:1 move out of the M10K pool -- a real synthesis-stage answer, not a proxy). Font ROM repack's actual win is **not** confirmed by this step and cannot be -- synthesis reports declared content bits, unchanged by construction (4x8-bit lanes = the same 97,280 bits as one 32-bit array), so whether it reduces physical M10K block count is a fitter question the synthesis-only stage cannot answer, contrary to section 10's original "the block-count drop is the entire result" claim. Corrected `docs/PHASE_F_SPEC.md` sections 10 and 14, and `docs/CURRENT_STATUS.md`, before proceeding -- owner asked for the docs fix first. VM staging left in place for reference (`tau-local/blit-step1-20260922`, `-baseline-20260922`), not cleaned up. Next: owner decides whether to proceed to step 2 (the real fit).
- 2026-09-22 (Claude): RTL/tools, no hardware yet. B-101: owner scoped step 2 down to "everything except the blit engine opcodes" (asked after I flagged that the spec's literal step 2 bundles new blit RTL that does not exist yet, and that the blit engine itself was earlier held for a fresh session). Built B7, the SDRAM busy-cycle counter (PHASE_F_SPEC.md section 5), independent of the blit opcodes since it only observes the existing single-port SDRAM arbiter. New `tau_cdc_gray_ctr.sv`: reusable parameterised Gray-code counter CDC (same technique mp3_fb.sv's own FIFO pointers already use), since the arbiter lives in clk_sdram (100 MHz) and MMIO reads happen in clk_sys (60 MHz) -- a genuine, previously-nonexistent cross-domain counter case in this codebase. Wired `~arb_p0_available` (sdram_fb's own idle signal; its inverse is exactly "the single SDRAM port is busy", independent of which master holds it) through the CDC into a new mp3_soc port (`sdram_busy_rd`, gated by new parameter `SDRAM_BUSY_ENABLE`) and MMIO offset 0xBC (`SDR_BUSY`, free-running, never cleared -- firmware samples before/after a window, same convention as CYCLES). Registered in `ap_core.qsf`; new `tools/blit_step2_qsf_append.txt`. Wrote `sim/tb_tau_cdc_gray_ctr.v` (unrelated clk_src/clk_dst rates, checks monotonicity and exact convergence) -- **also tried and removed a mutation test**, having found that a zero-delay functional simulator cannot reproduce the metastability/bit-tearing hazard Gray coding exists to prevent (a deliberately-broken raw-binary-sync variant passed the same bench with 0 failures, since iverilog always captures a register's whole input atomically); said so directly in both the module header and the testbench rather than keep a test that doesn't test what it claims. `make rtl-lint`, `make test-host` and `make test-rtl` all pass (0 failures); `mp3_soc_sim.v` regenerated correctly for the unrelated PSRAM testbenches, confirmed unaffected. No firmware/Check consumer built yet -- deliberately deferred, since the blit-storm Check test (section 12.1) that would use this counter is itself blit-engine-specific and out of scope for this step. Quartus fit (step 2 proper, on the VM) NOT yet launched.
- 2026-09-22 (Claude): Tau Omega (companion repo). Verified the companion project's health (`cargo test -p tau-core` 22/22, `npm run check` 0 errors), then at the owner's request `git init`'d it (it had no repository at all -- pure untracked work with no history) and renamed it from its working title "Tau Browser" to **Tau Omega** before the first commit: directory renamed, Cargo package `tau-browser` -> `tau-omega`, Tauri `productName`/window title/`identifier` (`dev.tau.browser` -> `dev.tau.omega`), npm package `tau-browser-ui` -> `tau-omega-ui`, and every doc/source/UI-string mention retitled; rebuilt (`cargo build` in `src-tauri`, `npm run check`) to confirm the rename didn't break anything before committing. Added `.DS_Store` to its `.gitignore`. Cross-referenced the two projects: this repo's `README.md` now points at `../Tau Omega/` (its own README already pointed back at `../tau-alpha/`). Single initial commit (99 files, 13 MB, mostly golden/synth test fixtures and the Space Grotesk font). Both repos are companion projects for the owner's planned future distribution website. This entry is the required per-turn log append for this file (project rule, section 3) even though the coding work landed in the companion repo, not here.
- 2026-09-22 (Claude): Tau Omega (companion repo), docs only. Audited the companion engine against the owner's portability goal and recorded the result; committed there as `b596dba`. **Targets settled:** Pocket Sync (Tauri + Rust + TypeScript, AGPL-3.0) is the named integration target and the boundary is a plain Rust **crate dependency** -- no plugin ABI, C ABI, WASM or sidecar, because the host is Rust and, like every comparable manager, has no plugin system, so adoption means an upstream contribution or a fork and the real goal is being easy to vendor. Feishin was assessed on the owner's suggestion and **dropped** (Electron/GPL-3.0, streams from Navidrome/Jellyfin/Subsonic so it has no local files to index, no plugin API, no Rust runtime, little audience overlap); the source-abstraction work that target would have forced is explicitly not being done. Licence boundary written down (D-010): Tau Omega stays MIT OR Apache-2.0, our code may flow into their AGPL, theirs must never be copied back -- the same one-way rule this repo already lives under with GPL RTL (B-085). **Audit result:** the engine internals are sound and better than its own architecture doc asked for -- three dependencies (crc32fast, serde_json, sha2; no tokio/async/GUI), no `println!`/`process::Command`/`std::env`, no global or static state, no thread spawning, plan/execute is already a portable data contract rather than the UI flow the docs describe, and index parsing verifies the body CRC before any offset-driven traversal (the byte-level unwraps in the tag/cover parsers were each checked and are guarded by prior length checks, so not latent panics). All the gaps are at the boundary: the `/Assets/<platform>/common/` prefix rule is implemented separately in the CLI and the Tauri adapter (which also owns index-status presentation, contradicting their D-001); warnings and errors cross every boundary as English prose, with `TauError::code()` called only by the conformance test while 34 sites flatten to strings -- the tests prove the E-codes are right and the product discards them; and there are no progress or cancellation hooks at all despite the architecture doc promising cancellable jobs. Also corrected that doc where it described a codebase that does not exist (module tree, the ~20-crate dependency list vs the actual three, the unbuilt cancellation claim, five extension points that exist only on paper) -- it matters because their plan drives a code-generating assistant from those documents. No firmware, RTL, card or VM touched.
- 2026-09-22 (Claude): Tau Omega (companion repo), read-only against this repo. Cross-checked the companion app's feature list, dependencies and format assumptions against **this project's current plan** (CURRENT_STATUS, PHASE_F_SPEC, ARCHITECTURE_ROADMAP, the 0.4 handoff, MEDIA_LIBRARY_0.4_SPEC, `tools/tau_data_slots.py`, `tools/decode_tau_suite.py`, and the shipped `dist/Cores/alfatreze.TAU/{core,data,interact}.json`); committed there as `ecade68`. This repo was only read -- no firmware, RTL, tools, card or VM touched. **Found and fixed a live bug in their engine:** library-capable detection read `data.json`'s `data` key as an array, but the real APF layout is `{"data": {"data_slots": [...]}}`, so our shipped v0.4.0 core -- which does declare slot 5 `tau-library.tdb` -- was reported as "legacy" and their whole media-library feature was gated behind a check that could never pass on hardware; it survived because their fixture had invented the flat shape, so fixture and code agreed while both disagreed with APF. **Two open questions land on THIS project, both live because the blit engine is the active item:** (1) **data slot 6 is double-booked** -- `docs/MEDIA_LIBRARY_0.4_SPEC.md` section 3 reserves slot 6 for `tau-library-art.bin`, but Phase G shipped the cold image `tau-cold.bin` in slot 6 and it is in the released core today, so the art file currently has no slot and one must be picked before thumbnails are built; (2) the art pixel format is still the spec's own "RGB565 assumed; **verify against `fw/art.inc` before freezing**", which is now due. Also confirmed and written down on their side: persist **ids 20-23 are overloaded** -- every core declares them `(internal) list 1..4` (legacy playlist state) and `tools/decode_tau_suite.py` reads those same four ids as the Check report, with no flag distinguishing them (the TAUD1 CRC32 is the only discriminator), and our own logs' "persist words 8-11" are *declaration positions*, which are ids 20-23 -- two numbering schemes for the same four values. Verified unchanged and correct: index v1 header/caps, no v2 planned, persist ids 24-27, data slot 5, and thumbnails deferred on both sides.
- 2026-09-22 (Claude): Tau Omega (companion repo), docs only, no firmware relevance. Assessed a third candidate integration host the owner proposed (`awesome-music-player` by S1avv) and recorded it as parked in their D-012, committed there as `26abd7f`. Best technical fit yet (Tauri 2 + Rust + React, MIT, offline-first local files, `lofty` tags -- their exact stack) but the weakest project (28 stars, 22 commits, three releases in a four-day burst in June then quiet, no plugin API and none planned), so not adopted; Pocket Sync remains the target and nothing in their plan changes. Two things worth keeping: being MIT makes it the one candidate they may legally borrow *from*, so it is kept as a reference for the in-app preview player they already wanted rather than as a host to plug into (owner's stated intent); and the general point that a Pocket sync feature is niche for a general-purpose player's users, so upstream contributions to general players are weak on merit regardless of how easy the integration is -- Pocket Sync is the exception because its users are Pocket owners.
- 2026-09-22 (Claude): VM, no card. B-102: step 2's real multi-seed fit (MLAB migration + font repack + SDRAM busy counter, no blit opcodes -- B-101's scope) run for real on the VM. **Font repack settled: +0 blocks, not the hoped +4** -- both seeds fit to 298/308 (baseline 300/308), a net +2 that's fully explained by the two MLAB items alone; on-chip repacking is not a usable lever for the font ROM, PSRAM is now the only real path to those blocks. Timing genuinely differs by seed, not just noise: seed 1 closed with a real -0.001/-0.101 ns setup violation traced to one exact path (px_color arithmetic into glyphbuf's MLAB write-data port); seed 2 closed positive on all four corners with identical RTL -- seed 2 selected, seed 1's violation recorded as a near-zero-margin finding worth re-checking if glyphbuf's feeding logic changes again. Also caught and fixed a real process bug: the first seed-2 attempt showed 3 errors from two `make fpga` invocations racing on the same directory (an earlier botched for-loop's leftover run colliding with my explicit relaunch, confirmed via impossible Assembler-before-Fitter log ordering) -- re-staged and reran cleanly, which reproduced the same 298/308 and closed positively, confirming it was a launch bug, not an RTL problem. Corrected `docs/PHASE_F_SPEC.md` sections 4 and 14 and `docs/CURRENT_STATUS.md`. No card install -- nothing to test on hardware yet (no blit opcodes, no Check consumer for B7). Separately, researched upstream HarpMudd v1.5.0's hardware-verified Japanese/CJK + UTF-8 text support (owner asked about eventual broader type support): reusable tooling and firmware string-pipeline hardening, but their design streams the extended font live from SDRAM, which this project would want in PSRAM instead given Phase F's whole SDRAM-contention focus. Parked in CURRENT_STATUS.md pending owner interest, not yet written up as a spec section.
- 2026-09-22 (Claude): RTL/sim, no hardware. B-103: owner said "start the blit engine" -- scoped to a complete, verified foundation (MMIO descriptor register file, section 9; B1 generalised blit, section 5) rather than shallow progress on all of Tier 1. Register file: `R_BLT_IDX`/`R_BLT_DATA` (0xC0/0xC4) for four sticky fields (SRC/DST BASE+STRIDE); deliberately reused the existing proven `R_FB_GO`/`fb_cmd_op` per-command path (widened 2->3 bits, claiming an already-unused padding bit) instead of adding a third new register, since spec's "three registers" was about minimal footprint and reuse achieves that better than duplication. `OP_BLIT`: extends `OP_COPY`'s exact streaming-write datapath with independent 25-bit source/dest addresses and sticky per-row stride instead of COPY's fixed FB_BASE=0/512 -- no multiplier needed, the per-row step was already a plain add. Deliberately did not fix COPY's existing 128-entry row-buffer width limit (that's an M10K cost decision of its own). Fail-safe came free: old RTL's 2-bit opcode read truncates the new op value to RUN, not a hang. Verified in `sim/tb_mp3_fb.v`: equivalence (defaults = byte-identical to COPY) and independence (custom base/stride match hand-computed addresses exactly), plus a real mutation test (`make test-rtl-fb-mutation`, BUG_IGNORE_BLIT_STRIDE) confirmed caught -- a genuine functional mutation, unlike B-101's CDC counter which turned out to be untestable that way. Found and fixed one real Icarus-only bug along the way (a forward reference in mp3_soc.v that Quartus tolerates but Icarus doesn't -- exactly the class sim/make_soc_sim.py's own docstring warns about). `make rtl-lint`/`test-host`/`test-rtl` all pass. Not done: B2-B6, TAU_BLIT_BLEND, no Quartus slot spent. Updated docs/MMIO_ALLOCATION.md, docs/PHASE_F_SPEC.md, docs/CURRENT_STATUS.md.
- 2026-09-22 (Claude): RTL/sim, no hardware. B-104: owner said "continue with B2 and B6" -- both built on B1's OP_BLIT/R_BLT_IDX/R_BLT_DATA foundation. B6 (OP_BAR, meter column): two chained RECT fills, no new burst mechanism -- the existing rect_active/A_WRWAIT loop runs twice per command, second segment queued and re-armed the instant the first's last row retires. cmd_addr = span top-left (existing convention), cmd_glyph (unused outside CHAR) = lit-row count clamped to height, cmd_fg/cmd_bg = lit/unlit colours (COPY's "no other use" precedent). Decided (not left implicit) that lit rows are the bottom of the span, documented in code. B2 (colour-key transparency): needed more than "one comparator" to be correct -- showing dest through a keyed pixel needs a dest READ, which OP_BLIT/OP_COPY never did (write-only). Added a real destination pre-read phase (new state A_KEYDST, a clone of the existing A_COPYRD read-into-glyphbuf loop but unconditional); the source read then just skips writing glyphbuf for a KEY-matching word, leaving the pre-read dest pixel in place -- no separate select/blend stage. OP_COPY stays untouched, never keys. New sticky field 5 (R_BLT_IDX=4: enable+colour); blt_idx widened 2->3 bits, wraps 4->0 for a clean 5-write burst load. Verified in sim/tb_mp3_fb.v: BAR (split/fully-lit/fully-unlit cases, exact addresses and colours per segment); B2 (one KEY-matching word of four keeps dest, the other three and a non-matching row take src normally, plus keying-disabled control case). Two new mutation parameters (BUG_IGNORE_KEY joins B-103's BUG_IGNORE_BLIT_STRIDE) both confirmed caught by make test-rtl-fb-mutation. make rtl-lint/test-host/test-rtl all pass, 0 failures. Not done: TAU_BLIT_BLEND, B3 (skew/masks), B4 (scaled blit), B5 (alpha blend, the -1.888ns risk item) -- no Quartus slot spent. Updated docs/MMIO_ALLOCATION.md, docs/PHASE_F_SPEC.md, docs/CURRENT_STATUS.md.
- 2026-09-22 (Claude): RTL/sim, no hardware. B-105: owner said "continue with B3 and B4." B3: analysed rather than built -- the Amiga barrel-shifter/word-mask mechanism section 5 describes is for bit-packed multi-pixel-per-word formats, and this engine is one pixel per SDRAM word, so the literal mechanism has nothing to translate to; what it would buy for a blit, B1's own generalised addressing already provides for free. The real gap (CHAR sub-glyph clipping, the marquee's actual limitation) needs a firmware change first -- checked fw/player.c and found fb_char() never writes R_FB_SIZE at all, so retrofitting cmd_w/cmd_h as CHAR clip fields would silently feed garbage leftover RECT/COPY dimensions into every existing glyph draw. Confirmed by reading the source, not assumed; recorded rather than built. B4 (OP_SBLIT, scaled blit): a brand-new opcode, so none of B3's compatibility risk applies. Reuses CHAR's own Bresenham registers directly (never both active at once) against a variable source size (cmd_w/cmd_h, safe here) instead of CHAR's fixed 16px cell; new sblit_ext() computes the output extent via a small multiply-by-constant, no divider. Every output pixel issues its own single-word SDRAM read (new state A_SBLIT), returning through A_IDLE between pixels like every other transaction -- scanout can preempt between any two words. Destination steps by the sticky stride every row (reusing B1's blit_dst_addr unchanged); source steps only when the Y-Bresenham condition fires. Same 127-word glyphbuf limit as OP_COPY/OP_BLIT. Verified in sim/tb_mp3_fb.v: 1x (agrees with the row-burst path pixel-for-pixel) and 2x (a 2x1 source doubles to 4x2 output, hand-computed values matched exactly on the first run). New mutation parameter BUG_SBLIT_NO_SCALE (silently drops to an unscaled copy) confirmed caught. make rtl-lint/test-host/test-rtl all pass, 0 failures (3 mutation cases total for this file now). Not done: TAU_BLIT_BLEND, B5 (alpha blend, the -1.888ns risk item), CHAR's sub-glyph clipping (needs the firmware coordination above). No Quartus slot spent. Updated docs/MMIO_ALLOCATION.md, docs/PHASE_F_SPEC.md, docs/CURRENT_STATUS.md.
- 2026-09-22 (Claude): RTL/sim, no hardware. B-106: owner said "continue with B5" -- the last Tier 1 item, kept behind its own TAU_BLIT_BLEND macro (separate from TAU_BLIT) per section 10's build plan, since it's the deepest new pipeline and carries the documented -1.888ns timing-cliff risk. Caught a real gap before shipping: built the blend datapath first, then found mp3_fb's existing instantiation in core_game.vh passed NO module parameters at all -- meaning BLIT_BLEND_ENABLE would have stayed 0 regardless of the new macro, making the whole feature permanently unreachable. Fixed: parameterized the instantiation plus the TAU_BLIT_BLEND->TAU_BLIT_BLEND_EN derivation (requires TAU_BLIT). Second real bug found while generalizing B2's destination pre-read trigger to also fire for blend: the existing key-match check relied on key_dst_done implying blt_key_en was on, which stopped being true once blend could trigger the same pre-read -- fixed with an explicit blt_key_en check (new pixel_keyed wire) rather than the old implicit assumption. Blend itself: new sticky field 6 (DSP 0-255 alpha via >>8 approximation, or one of 4 PSX shift-add ratios, all clamped not wrapped), one shared blend_ch function for R/G/B. Verified in sim/tb_mp3_fb.v: DSP alpha=128 reduces to an exact per-channel average (hand-computed values matched first try); PSX B+F mode deliberately overflows a channel to confirm clamping. New mutation parameter BUG_BLEND_ALWAYS_SRC confirmed caught. make rtl-lint/test-host/test-rtl all pass, 0 failures (4 mutation cases for mp3_fb now, 7 total). Tier 1 is functionally complete in RTL/simulation as of this entry (B1/B2/B4/B5/B6 built, B3 analysed and correctly scoped out). Not done: the actual Quartus fit that would show whether the timing-cliff risk is real -- simulation can't answer that. Updated docs/MMIO_ALLOCATION.md, docs/PHASE_F_SPEC.md, docs/CURRENT_STATUS.md.
- 2026-09-22 (Claude): VM. Owner said "go ahead and launch it on the VM" -- step 2 of section 10's build plan, the first real Quartus spend on the blit engine itself. New tools/blit_step2_full_qsf_append.txt bundles MLAB migration + font repack + the SDRAM busy counter + the full blit engine (TAU_BLIT + TAU_BLIT_BLEND) per section 10's original step-2 definition, so timing reflects the real final bitstream rather than blit-in-isolation. Staged and launched two seeds (blit-engine-s1-20260922, blit-engine-s2-20260922), verifying with pgrep that each launch was a single clean process before starting the next -- the discipline learned from B-102's process-race corruption earlier this session. Both confirmed running (quartus_fit at real CPU%%, not just the parent shell). Result pending; check make_fpga.log and, once done, ap_core.fit.summary + the four-corner timing slack in each directory before trusting either result. Watches specifically for the documented -1.888ns timing-cliff risk (PHASE_F_SPEC.md section 11); if it fires, the documented first bisect is dropping TAU_BLIT_BLEND alone.
- 2026-09-23 (Claude): docs only (B-108). Parked MOD/tracker format support as a feature idea per owner request: new docs/MOD_TRACKER_SUPPORT_SPEC.md (software-first mixer, toggleable Amiga output filter with the cited "~14 kHz" figure flagged OPEN pending a verified source, quantization effect flagged as needing disambiguation -- linear 8-bit vs. non-linear companding are different features -- tracker pattern-view UI gated on the Phase F blit engine, plus additional opportunities: format-family scope, an effect-compatibility test corpus, library/metadata integration, shared resampling-quality setting, per-channel mute/solo); referenced from docs/ARCHITECTURE_ROADMAP.md's parked-items list after Phase D. Independent of, and does not touch, the in-flight blit-engine VM builds (B-107). No code, RTL, card or VM touched.
- 2026-09-23 (Claude): VM/docs (B-109). B-107's two blit-engine VM builds finished: both seeds Successful (0 errors, RAM 298/308), but both fail setup timing on both Slow corners (-2.5 to -2.9 ns), worse than the -1.888 ns figure PHASE_F_SPEC.md section 11 had been citing. Ran `quartus_sta -t` with `report_timing -setup -detail full_path` on seed 2 to find the actual violating path, since simulation can't answer this and the documented bisect assumed a specific cause -- found the violation is entirely inside `glyphbuf`'s existing MLAB write-data arithmetic (the exact near-zero-margin path B-102 already flagged, before the blit engine existed), not on the new `TAU_BLIT_BLEND` compose pipeline section 11 blamed. Corrected PHASE_F_SPEC.md sections 10 and 11 and CURRENT_STATUS.md before recommending anything further, rather than leave the disproven prediction standing. No bisect build launched -- flagged for an owner decision (try the documented "drop TAU_BLIT_BLEND" bisect anyway vs. go straight for pipelining the actual flagged path). No card write, no package, no install.
- 2026-09-23 (Claude): VM (B-110). Owner said "let's try one and see what comes out from it, then decide from there" -- launched the documented bisect (new tools/blit_step2_noblend_qsf_append.txt, same bundle as B-107 minus TAU_BLIT_BLEND, diffed to confirm the only difference), single seed (seed 2) since this is a yes/no experiment answering whether less logic relieves the glyphbuf congestion B-109 found, not a card-candidate fit. Staged via git archive HEAD (e501c7c, same commit B-107/B-109 built from) into tau-local/blit-noblend-s2-20260923, confirmed via ps that the new quartus_map is the only active build (two other Quartus-named processes in the list are stale idle monitor loops from earlier sessions, not competing compiles). Result: build Successful, setup slack recovered from -2.5/-2.6 ns to +0.023/-0.112 ns -- the bisect worked far better than expected, but the worst path moved to a different pre-existing marginal path (CHAR's own cmd_q/char_fg colour-computation arithmetic, unrelated to blit/blend) rather than closing fully, leaving a small -0.1 ns gap. Interpretation: the device has multiple genuinely marginal timing paths, not one bug -- removing TAU_BLIT_BLEND's congestion fixed the worse one and exposed the next-worst. Updated docs/AUDIT_TRAIL.md B-110 with the full result. No card write. Owner said "yes, launch the second seed" -- staged and launched seed 1 with the same no-blend config (tau-local/blit-noblend-s1-20260923), diffed to confirm only SEED differs, confirmed no build collision via ps. Result: seed variation does NOT close the -0.1 ns gap -- seed 1 lands at -0.095 ns (Slow 0C), essentially the same as seed 2's -0.112 ns, confirming a real structural near-miss rather than seed noise. Updated docs/AUDIT_TRAIL.md B-110 with the comparison and the evidence-based conclusion. Next recommended step: pipeline the char_fg/cmd_q CHAR-colour path directly (one register stage). No RTL change made, no further build launched, no card write -- owner decision pending.
- 2026-09-23 (Claude): RTL/sim (B-111). Owner said "yes" to making the targeted RTL fix and verifying in simulation. Traced the actual failing logic in src/fpga/core/mp3_fb.sv: B6 (BAR, B-104)'s lit/unlit row-split clamp+subtract, computed combinationally from cmd_q straight into the char_fg register in the same cycle -- a genuinely tight pre-existing path that TAU_BLIT_BLEND's congestion had been burying under the (now-fixed) glyphbuf violation. Fix is retiming, not new logic: added cmd_mem_rd (the same raw BRAM read cmd_q registers from) and a second register pair (q_bar_lit/q_bar_unlit) latched on the same clock edge as cmd_q from the same source data, so OP_BAR's dispatch now sees already-valid registers instead of a compare-subtract-compare chain -- same function, same cycle timing relative to cmd_q, bit-for-bit unchanged BAR behaviour. Cost: ~18 flip-flops and one duplicate comparator/subtractor, no new M10K/MLAB. make rtl-lint, make test-rtl (tb_mp3_fb's full BAR test suite plus all 4 existing mutation cases unchanged) and make test-host all pass, 0 failures. No Quartus build launched yet -- simulation proves correctness, not timing; next step is a quick fit check. No card write.
- 2026-09-23 (Claude): docs/audit (B-112). Owner asked for a full audit and review of older RTL, the current plan, and other feature risks/rework opportunities. Ran three parallel research passes (RTL timing pattern, firmware bugs/drift, SDRAM/audio invariant coverage) plus a direct plan/docs review; wrote up as docs/FULL_AUDIT_2026-09-23.md. Headline: B-111's BAR fix was one instance of a pattern -- OP_SBLIT's dispatch has the identical structural bug (same source register, never retimed), recommended the same fix; also found the blend write-back into glyphbuf (TAU_BLIT_BLEND-only) may mean B-110's "congestion relief" theory should actually be "direct fix" -- flagged as unverified either way, cheap to check before trusting the bisect again. Firmware: clean, but escalated the B-082 boot-restore mismatch (parked as UX friction) since the two builds share identical code for that behaviour with no known mechanism for the divergence. SDRAM/audio: blit-storm Check test and busy-counter consumer confirmed correctly deferred (zero firmware issues blits yet, so low current exposure), L0 track record clean. Fixed one doc drift (ARCHITECTURE_ROADMAP.md described a never-built R_BLT_GO register; MMIO_ALLOCATION.md already had the as-built truth). No RTL/firmware changes made -- recommendations only, owner decision pending on what to action.
- 2026-09-23 (Claude): docs only (B-113). Owner said "add these to the roadmap where most convenient as well as any other required docs" -- folded B-112's audit findings into the standing documents instead of leaving them only in the one-off audit doc. Rewrote PHASE_F_SPEC.md sections 10/11/14 (timing-cliff risk row, build-plan bisect paragraph, order-table step 3) to carry the full B-109..B-112 story instead of stopping at stale snapshots; updated CURRENT_STATUS.md's blit-engine narrative through B-110/B-111/B-112; added a status pointer under ARCHITECTURE_ROADMAP.md's Phase F header; created docs/issues/021-boot-restore-release-vs-diagnostic-mismatch.md (matching the existing 001-020 issue convention) for the escalated B-082 item, with a suggested next step that explicitly avoids repeating B-073's mistake of guessing from source alone; updated SESSION_HANDOFF_2026-09-22_RELEASE_0.4.md's two B-082 mentions to point at the new issue. No RTL/firmware changes, no card/VM touched -- purely making the audit's findings discoverable from the documents someone would actually open next.
- 2026-09-23 (Claude): RTL/sim (B-114). Owner said "next step" -- applied B-111's proven retiming technique to the two other instances of the same bug shape B-112 found: OP_SBLIT's output-extent compute (sblit_ext off raw cmd_q, same as the fixed BAR bug) and OP_CHAR's char_base compute (a milder variant). Both retimed off cmd_mem_rd on cmd_q's own clock edge; removed the now-genuinely-dead q_glyph wire (its only use was the old char_base expression). make rtl-lint, make test-rtl (tb_mp3_fb's full CHAR/SBLIT suite plus all 4 mutation cases unchanged) and make test-host all pass, 0 failures. Blend/glyphbuf write-port theory from B-112 still unverified; no Quartus build launched yet -- next step is a re-fit of no-blend + B-111 + B-114 to confirm the remaining -0.1 ns gap closes. No card write.
- 2026-09-23 (Claude): docs only (B-115). Owner said to leave the boot-restore issue (docs/issues/021) parked, and separately wants to revisit the full UI design and rethink the UX from the ground up -- this specific issue might not survive that redesign. Updated the issue file and CURRENT_STATUS.md to record this as a deliberate re-park (not a reversion to the old "UX friction, not blocking" framing B-112 already showed was unsupported), and added a new "UI/UX redesign" section to ARCHITECTURE_ROADMAP.md after Phase E recording the intent, cross-referencing items likely superseded by it (boot-restore mismatch, missing loading message, browse-position memory, meter/visualizer split, tracker viz), and recommending the redesign's spec wait on the blit engine's primitives before finalizing. No redesign spec written -- intent recorded only, per the owner's own "not yet an issue" framing. No code/card/VM touched.
- 2026-09-23 (Claude): docs (B-116). Owner asked for "the verification" -- resolved B-112's open question of whether dropping TAU_BLIT_BLEND fixed the original glyphbuf violation directly (blend write-back widening the write-data mux) or merely relieved congestion as B-110 first guessed. Ran report_timing against the still-present full-blend build database on the VM (no re-fit needed, database from B-107 was never cleaned up): all 10 worst violated paths are the identical Add32~8/Selector222~1 chain B-109 found, zero blend-related cells anywhere in them. Confirmed mechanism: Add32~8 is DSP-mapped, and TAU_BLIT_BLEND's own DSP usage (14 vs 11 without) competes for placement/routing in the same region -- congestion relief, not a direct fan-in fix. Corrected PHASE_F_SPEC.md section 11 and CURRENT_STATUS.md, which both still said "unverified either way," to reflect the confirmed result. The only remaining open timing step is a re-fit of the no-blend + B-111 + B-114 combination. No RTL change, no card/VM build launched -- read-only query against an existing fit.
- 2026-09-23 (Claude): VM (B-117). Owner said "yes, launch it" -- the re-fit combining the no-blend config with B-111/B-114's retiming fixes. Since mp3_fb.sv's fixes are uncommitted, used git stash create (working tree untouched, no stash entry stored) instead of git archive HEAD to stage the actual current RTL, verified by grepping the staged copy for q_bar_lit/q_sblit_out_w/q_char_base before launching. Same macro set as B-110's no-blend qsf (diffed to confirm), seed 2, single seed matching the yes/no-experiment convention. Confirmed no build collision via ps. Result pending.
- 2026-09-23 (Claude): research/skill (B-118). Owner asked to investigate the core community for prior findings on the DSP-placement-congestion issue and put good findings into the skill. Web search found nothing openFPGA/Analogue-Pocket/MiSTer-specific for this exact pattern -- genuinely unexplored territory as far as public search shows. Found real, applicable general Quartus/Intel guidance instead: a per-instance DSP_BLOCK_BALANCING assignment can force a specific adder off DSP mapping without touching other DSP usage (directly relevant to B-116's Add32~8 finding), BRAM-to-DSP timing bottlenecks are a documented Intel pattern with pipeline-register as the standard fix (validates the retiming technique already used 3x this session), and LogicLock regions (which I'd suggested earlier) are generally the wrong tool per FPGA placement literature -- corrected my own prior suggestion. Wrote up as KB-045 in the analogue-pocket-dev skill (docs-verified, not hardware-validated -- neither technique tried on this project yet), kb.py validate/index pass (45 entries, 0 problems). Research only, no code/card/VM touched; the in-flight re-fit (B-117) is unaffected.
- 2026-09-23 (Claude): research/skill/docs (B-119). Owner asked to widen the research to general Intel/Quartus documentation and community knowledge on the project's critical performance/resource-management topics, not just the one alpha-blend question. Found and recorded two genuinely actionable items: Quartus Rapid Recompile (KB-046) -- no partition setup needed, ~65% average compile-time reduction for small isolated RTL changes, directly relevant given this session ran ~15 full Quartus fits mostly touching only mp3_fb.sv -- added to ARCHITECTURE_ROADMAP.md's Tooling track as item 5; and M10K native-width packing (KB-047) -- a memory array's per-word width matters independently of its total size being a power of two, directly relevant to the still-unexecuted Phase G RAM shrink -- added to the Phase G section as a design-time check to do before fixing array widths. Also checked multi-seed timing practice and SDRAM arbitration priority against literature -- both confirmed the project's existing approach is already sound, nothing new to record there. No openFPGA/Analogue-Pocket/MiSTer-specific writeups found for any of this, consistent with B-118. kb.py validate/index pass (47 entries, 0 problems). Research and docs only, no code/card/VM touched; the in-flight re-fit (B-117) unaffected.
- 2026-09-23 (Claude): research/skill/docs (B-120). Owner asked for deeper Quartus/Cyclone V research generally, not scoped to the Pocket community. Headline finding: grepped src/fpga/ap_core.qsf and confirmed FITTER_EFFORT is set to "AUTO FIT", which Intel's own docs say explicitly stops optimizing once "good enough" and skips timing-affecting optimizations to save compile time -- Standard Fit keeps optimizing for best Fmax regardless of time. Given this whole session (B-107..B-117) chased sub-nanosecond violations via manual RTL retiming, this is a real testable lever that might have been leaving margin on the table the whole time -- written up as KB-048 with the honest 2x+ compile-time trade-off stated. Also confirmed the MLAB chaining math (32x20/640-bit blocks, so 256/32=8 chained instances) with a real citation, replacing a pure [EST] guess in PHASE_F_SPEC.md's risk table (the Fmax penalty itself stays [EST], only the depth math is now confirmed). DSP-inference-threshold and VexRiscv-Fmax searches didn't yield project-actionable findings. kb.py validate/index pass (48 entries, 0 problems). Separately: lost SSH access to the Quartus VM partway through (connection refused, then timed out) -- could not check the in-flight B-117 re-fit result, which is still unknown; flagged for the owner, needs VM/tunnel attention on their end. No code/card/VM touched by this entry itself.
- 2026-09-23 (Claude): VM. Owner's Mac had a power loss that took the VM down mid-fit; VM restarted, owner logged in. Confirmed via uptime the VM is fresh; confirmed B-117's original in-flight fit was killed (no process, no fit.summary, log cut off mid-placement-prep) -- that result is lost, not recoverable. Working tree unaffected by the outage (mp3_fb.sv's B-111/B-114 fixes still uncommitted and intact). Relaunched the identical no-blend + B-111 + B-114, seed 2 configuration from scratch: fresh git stash create, re-verified the fixes landed in the staged copy, qsf diffed byte-for-byte against the working reference to confirm zero drift. Confirmed single clean quartus_map process on the fresh boot. Deliberately did NOT add STANDARD FIT (KB-048) to this relaunch -- keeping it a clean continuation of the original question before layering in an untested variable. Result pending, same 50 min-1h45m wait as before.
- 2026-09-23 (Claude): docs/git (B-121). Owner said to commit the research findings to project knowledge and the skill, reconsider what they change in ongoing/planned work, and add new tasks where they'd improve the project. Checked the skill's own repo first -- local-entries/ and INDEX.local.md are deliberately git-ignored there (project-private knowledge stays local by design), so KB-045..048 are already correctly saved via kb.py, no git action needed or appropriate. Committed the Tau-Alpha repo's accumulated docs work in two commits (78b0b7f: MOD tracker spec/full audit/issue 021 as cleanly-separable new files; 70f3fcc: the modified planning docs carrying the whole session's B-109..B-120 progression, which couldn't be split further since each file mixes multiple entries). Did NOT commit the uncommitted mp3_fb.sv RTL fix (B-111/B-114) -- staying consistent with verify-before-commit, since the in-flight re-fit hasn't confirmed it yet. Added a new explicit "Timing-experiment backlog" to PHASE_F_SPEC.md section 14 (4 ranked items: STANDARD FIT, per-instance DSP_BLOCK_BALANCING, Rapid Recompile, M10K width check) so these are discoverable as concrete next actions, not just risk-table prose; also fixed a stale step-3 row that still said OP_SBLIT wasn't fixed. No RTL/firmware changes, no card touched.
- 2026-09-23 (Claude): docs (B-122). Owner asked for a README section for core developers covering high-level issues faced and their solutions, a list of useful/new technical approaches, and a link to the analogue-pocket-dev-skill on GitHub. Added "For core developers" between "Known limitations" and "Credits": five issue/lesson entries framed generally (recognizable by a developer on a different Cyclone V project, not just Tau-specific narration), a techniques table reusing the project's own evidence-grading vocabulary (docs-verified vs hardware-validated framed in plain language) so it stays consistent with AUDIT_TRAIL.md and the skill's KB status field, and the confirmed public skill URL (github.com/alfatreze/analogue-pocket-dev-skill, from this project's own B-020-era publish record, not guessed). Kept full narrative detail out of the README, pointed at AUDIT_TRAIL.md B-109 through B-121 once. No code/card/VM touched.
- 2026-09-23 (Claude): git (B-123). Owner said "let's publish in the meanwhile" while the B-117 relaunch re-fit continued running. Reviewed the 10 commits ahead of origin/main before pushing (blit-engine B1-B6 feature commits plus this session's four docs commits) -- nothing sensitive. Pushed origin/main 8311d36..ac39472. src/fpga/core/mp3_fb.sv (B-111/B-114's RTL fix) stays uncommitted and unpushed, consistent with verify-before-commit, pending the in-flight re-fit's result.
- 2026-09-23 (Claude): VM (B-117 final). MILESTONE: the relaunched re-fit (no-blend + B-111 + B-114, seed 2) finished Successful and closed cleanly on every corner -- Slow 85C +0.727 ns, Slow 0C +0.597 ns setup slack (vs -0.112 ns before the fixes), real margin not a bare pass. RAM 298/308, DSP 11/66, matching every prior no-blend fit exactly. First build in the whole B-107..B-117 sequence with zero known timing violations on any corner. Updated docs/AUDIT_TRAIL.md B-117 (final), docs/PHASE_F_SPEC.md (risk table + order table, both now read RESOLVED/CLOSED for this config), docs/CURRENT_STATUS.md (milestone callout). Next: confirm with a second seed before final adoption, then commit the RTL fix (still uncommitted). TAU_BLIT_BLEND stays shelved pending its own fix. No card write.
- 2026-09-23 (Claude): git (B-124). Owner said "let's skip the seed[,] don't jinx it" -- declining the second-seed confirmation, committing the RTL fix as-is. Committed src/fpga/core/mp3_fb.sv (612698f) with the full B-111/B-114 retiming story and the B-117 hardware timing result in the message. Stated the judgement call plainly in AUDIT_TRAIL.md B-124: the margin (+0.6-0.7 ns) is large relative to observed seed-to-seed spread on borderline cases, but this is accepted risk, not verified safety. Not pushed yet -- pending an explicit publish instruction, not assumed from the commit alone.
- 2026-09-23 (Claude): sim/tools (B-125). Owner said "start on the reference renderer" -- PHASE_F_SPEC.md section 12's software reference renderer + pixel-diff fixtures. Built tools/host/blit_reference.py (every mp3_fb.sv opcode reimplemented from scratch in Python, CHAR's glyph data parsed directly from the shipped font_rom.v rather than re-rasterised, avoiding a PIL dependency), sim/tb_blit_scene.v (an 11-command scene through the real cmd_push interface, harness adapted from tb_mp3_fb.v), and sim/test_blit_reference.py (drives both sides, diffs exactly, confirms all 4 existing mutation hooks are caught -- reused, not reinvented). Wired into make test-rtl as test-rtl-blit-reference. Found and fixed two real testbench-modelling bugs while building it: the SDRAM stub had no persistent memory, so a keyed/blended BLIT's destination pre-read never saw an earlier command's real writes (added real_mem); and the scene initially had no non-default sticky stride, so BUG_IGNORE_BLIT_STRIDE had nothing to diverge on (added a 10th scene command with dst_stride=96/src_stride=64). make test-rtl and make test-host both pass, 0 failures. Updated docs/PHASE_F_SPEC.md section 12 (marked done) and docs/CURRENT_STATUS.md. No card/VM touched -- host-side simulation only.
- 2026-09-23 (Claude): firmware (B-126). Owner said "commit only then continue" -- continuing to section 12's other half, the COLD_READY()-style fail-safe. Real finding before writing code: read core_game.vh/mp3_soc.v/mp3_fb.sv and found TAU_BLIT doesn't actually gate the blit engine's RTL at all -- no ifdef in mp3_fb.sv, mp3_soc.v's own comment says the register file exists "regardless of BLIT_ENABLE." No bitstream feature bit exists the way PSRAM/cold-code have one; every bitstream since B-103 already has the opcodes unconditionally. Built fw/blit_probe.inc's BLIT_READY() around the real distinction instead: an old (pre-B-103) bitstream's 2-bit opcode decode silently truncates OP_BLIT to OP_RUN, detected via a 2-row custom-stride BLIT into never-displayed framebuffer padding (columns 400-511) plus a sentinel readback through the CPU's proven uncached-window idiom. New TAU_BLIT_PROBE macro (default off), new player-blit-probe build target in fw/build.sh, wired into player.c's boot sequence. Confirmed byte-identical product ROM (git status shows no diff after rebuilding player). make test-host and make test-rtl both pass. Stated the verification limit plainly: the "custom stride honoured" half is indirectly proven by B-125's own scene test, the "old 2-bit decode truncates" half rests on B-103's documented claim, not a fresh test -- no real-CPU-in-the-loop simulation built for this specifically. This closes PHASE_F_SPEC.md section 12 in full. No card/VM touched.
- 2026-09-23 (Claude): docs/skill only. Validated the Quartus edition constraint (Cyclone V: Lite and Standard yes, Pro no, so "only Lite" is not exact) from a distributor copy of Altera's chart and this project's own Lite 25.1 fit report (retiming applied, 1,132 registers retimed); added toolchain drift tools and a general constraints reference to the skill, corrected four overclaimed local KB entries, and rewrote the weekly updater with a Quartus/Intel/Cyclone V track plus edition-gating rules. Skill changes uncommitted in the skill checkout. No code, card or VM touched.
- 2026-09-23 (Claude, weekly refresh): docs/skill only. Toolchain chart unchanged (Cyclone V: Lite yes, Standard yes, Pro no); 0 Analogue doc pages changed; 2 repos moved (openfpga-library metadata; plasticbugs template methodology, Codeberg tutorials still unreachable). 12 searches (6 Pocket, 6+ toolchain), 3 new community-reported entries KB-051..053 (fit deterministic per seed/build_id stamp, byte-enable inference pitfall, FAST_*_REGISTER vs SignalTap lead; primary page not read), notes on KB-011/052/053, kb.py validate passes (53 entries, 0 problems). No code or upstream refs touched.
- 2026-09-23 (Claude): firmware (B-127). Owner said "save all findings and state to documentation. Continue from where you left off" -- continued to the next open item, PHASE_F_SPEC.md section 12.1: the blit-storm Check test and a firmware consumer for the B7 SDRAM busy-cycle counter. New `CT_BLT` (fw/suite.inc), added to STANDARD/FULL/ENDURANCE: full-height OP_BLIT commands into the never-displayed framebuffer strip (columns 400-511, the same safe area B-126's probe proved), re-issued via a non-blocking poll (not fb_wait(), so the test cannot itself manufacture an underrun); verdict is late underruns, with the SDRAM busy permille over the window carried as the test's value -- the first real consumer of R_SDR_BUSY (MMIO 0xBC), gated behind a new TAU_SDRAM_BUSY macro (default off, matches whichever bitstream is installed; reports N/A rather than a false 0% when off). R_BLT_IDX/R_BLT_DATA/FB_OP_BLIT promoted from blit_probe.inc into player.c's shared register block. No new wire protocol -- rides the existing SR_T_TEST per-test value field, so suite_core.h/decode_tau_suite.py/test_suite.py are untouched. make test-host and make test-rtl both pass; player-library-diagnostic-profile builds clean; the release (player) ROM confirmed byte-identical (CT_BLT is entirely #if CHK_DEV). Honest scope note recorded in AUDIT_TRAIL.md B-127: this is B1 (generalised blit) load only, 112 of 512 columns, not literally full-screen and not B4/B5 (scaled/blended) traffic, since blend is shelved and no scaled-blit firmware helper exists yet. Not run on hardware -- needs the B-117 no-blend bitstream fitted/packaged/installed with -DTAU_SDRAM_BUSY=1 added to the firmware build, which hasn't happened. Docs updated: AUDIT_TRAIL.md (B-127), PHASE_F_SPEC.md (section 12.1 marked done with the scope correction, section 14's order-table row 3 updated), CURRENT_STATUS.md. No card/VM touched.
- 2026-09-23 (Claude): VM/tools/packaging (B-128). Owner said "next step" -- acted on B-127's own "Not done" item. Found the actual B-117 relaunched re-fit (`~/tau-local/blit-refit-s2-20260923`) still sitting on the VM with its RBF never copied off; reconfirmed its fit.summary matches what B-117 final already recorded and copied the RBF locally (SHA-256 verified). Built player-library-diagnostic-profile with a new opt-in `SDRAM_BUSY=1` override (fw/build.sh, default 0 -- deliberately not the default, since R_SDR_BUSY reads a hardwired 0 on any other bitstream and turning this on against the wrong one would report a false 0% instead of CT_BLT's N/A sentinel). Extended tools/package_sdram_stress.py's --diagnostic-profile mode to accept --rbf/--rbf-sha256 (it previously assumed the current dist/ bitstream unchanged, true for B-086 but wrong for this pairing) with the same audited-hash requirement other modes already enforce. Packaged as TAU DEV 40 (work/diagnostics/tau-dev-40/pocket); check_tau_package.py PASS. Not installed on the card yet -- asking the owner first, per section 2b/3's own SD-card-write rule.
- 2026-09-23 (Claude): card (B-129). Owner said "Yes, go ahead and install it." Found the card already had `alfatreze.TAU_DEV_42` (a decoder-profile build from a session not reflected in this file's own log) -- renumbered the package to **TAU DEV 43** rather than reuse 40/41/42, per the standing "numbers keep increasing" rule. Backed up TAU_DEV_42 (Cores/Assets/Platforms) to the session scratchpad, verified byte-identical before removing anything. Installed TAU_DEV_43 with the B-117 no-blend bitstream and the SDRAM_BUSY=1 firmware build; bitstream/ROM/cold-image SHA-256 all verified identical to the local package after copying. Carried TAU_DEV_42's existing test media (Nausicaa albums, Test Album, flac-tests, playlists, its tau-library.tdb) across unchanged so CT_AUD/CT_BLT have something to play; cleaned the AppleDouble junk `cp -a` left behind. Removed TAU_DEV_42 (Core/Assets/Platform entries + its stale Browser MRU cache file) after the backup verified. Card ejected cleanly. Cores on the card: TAU, TAU_DIAGNOSTIC, TAU_DEV_43. Not yet run -- first hardware verdict on the blit engine, BLIT_READY(), and R_SDR_BUSY is the owner's next boot.
- 2026-09-23 (Claude): card/docs (B-130). Owner reported TAU_DEV_43 unusable: starting screen, no playlist, no menu access, nothing loaded. Root cause found by reading every blit-engine qsf append file against the checked-in base: the ENTIRE B-100..B-117 blit-engine bitstream series (including B-117, the "closed timing" milestone) was built with only TAU_MLAB_MIGRATE/TAU_FONT_REPACK/TAU_SDRAM_BUSY/TAU_BLIT over the bare USE_SDRAM=1 base -- never with the shipped v0.4.0 bitstream's own TAU_PHASE2_WINDOW/TAU_PSRAM_PROBE/TAU_PSRAM_WINDOW/TAU_PSRAM_IFETCH macros the product firmware actually depends on for the library, playlist (no BRAM fallback since A-105) and PSRAM-cold-code menus/settings. The fail-safes worked correctly (nothing crashed) but with no fallback path left, "gracefully degraded" looked identical to broken. Card fixed: removed TAU_DEV_43, restored TAU_DEV_42 from its verified B-129 backup -- card back to exactly TAU/TAU_DIAGNOSTIC/TAU_DEV_42, unchanged from before B-127. TAU and TAU_DIAGNOSTIC (the real release cores) were never touched and were fine throughout. Real consequence: B-117's timing-closed result is NOT proven for the product configuration -- a genuinely new "full G3 macros + blit engine" fit has never been run and is needed before this bitstream family is trusted again. Documented in full: docs/AUDIT_TRAIL.md B-130, docs/PHASE_F_SPEC.md (row 3 caveated), docs/CURRENT_STATUS.md.
- 2026-09-23 (Claude): tools (B-131). Owner asked to start tagging feature-milestone builds to expected semantic releases instead of an ever-increasing TAU_DEV_NN. Confirmed via AskUserQuestion: semver pre-release naming (0.5.0-alpha.1, incrementing .N per install, alpha->beta->release). Added --semver to tools/package_sdram_stress.py (mutually exclusive with --number): sanitizes the version string into a Pocket-legal platform id/shortname (dots/dashes->underscores, alpha/beta->a/b, <=15 chars, enforced at packaging time), keeps the real semver string in title/description/core.json's version field. --number/TAU_DEV_NN kept for throwaway bring-up iterations not worth a version bump. Verified with a syntax check and a dry-run package (deleted after, not a real milestone). The correctly-scoped redo of TAU DEV 43 (full G3 + blit engine fit, still unbuilt per B-130) will be the first real use: 0.5.0-alpha.1.
- 2026-09-23 (Claude): VM/packaging (B-134). Both blit-g3 fits (B-132) finished Successful, 0 timing failures on either seed: seed 1 setup +0.634/+0.501 ns, hold +0.322/+0.305 ns; seed 2 +0.395/+0.310/+0.311/+0.302 ns; both 298/308 RAM, 11/66 DSP, same as the blit-only fits (B-102/B-117) -- the PSRAM/window logic added only ALMs/registers, no new block RAM/DSP pressure. Seed 1 selected (best on all four corners). This resolves B-130's open question: B-111/B-114's retiming survives combination with the real product configuration on the first attempt, no further RTL change needed. RBF copied off the VM (SHA-256 verified). Built player-library-diagnostic-profile with SDRAM_BUSY=1 (ROM unchanged); packaged with the new --semver flag as 0.5.0-alpha.1 (alfatreze.TAU_0_5_0_A_1, the first real use of B-131's naming convention); check_tau_package.py PASS. Not installed -- awaiting owner confirmation, doubly warranted after B-130.
- 2026-09-23 (Claude): card (B-135). Owner said "install." Re-checked bitstream macros against firmware requirements one more time first (the exact check that would have caught B-130) -- full match. Installed TAU_0_5_0_A_1 additively alongside TAU/TAU_DIAGNOSTIC/TAU_DEV_42 (all untouched); bitstream/ROM/cold-image SHA-256 verified after copy; carried TAU_DEV_42's test media across. Card ejected cleanly. Not yet run -- first correct hardware verdict on the blit engine is the owner's next Check.
- 2026-09-23 (Claude): card/docs (B-136). Owner reported an album wouldn't load on TAU_0_5_0_A_1; screenshot showed the app's own designed "nothing loaded" empty state (B-080), not a crash -- the always-on diagnostic stress HUD row at the bottom (E0 L0 M0 S0 R0 K0.0) was a red herring, confirmed expected by reading stress_hud_draw(). Menu/library browsing worked fine (B-130's fix holding), only opening a track failed. Root cause: my own mistake, not a blit-engine bug -- the tau-library.tdb carried over from TAU_DEV_42 (B-135) still has its root field baked in as /Assets/tau_dev_42/common/, a different core's folder, so track opens silently failed while browsing (which only needs the index's own embedded strings) worked. Fixed with tools/sync_media.py --from-core alfatreze.TAU_DEV_42 --core alfatreze.TAU_0_5_0_A_1 --library (dry-run confirmed all 47 media files already identical, only the index needed rebuilding); verify now OK with the correct root. Recorded the lesson: every "carry media across" install needs to rebuild the library index for the new core's own path, not just copy the old one -- this has now bitten twice (implicitly at B-129/B-130, explicitly here). Not yet re-verified on hardware.
- 2026-09-23 (Claude): VM (B-132). Owner said "Go ahead and run the full G3 + blit engine fit." New tools/blit_g3_qsf_append.txt combines the exact G3 staging (psram_window_qsf_append.txt + psram_ifetch_qsf_append.txt, confirmed against the shipped v0.4.0 rbf_r's own recorded build) with the no-blend blit macros (TAU_MLAB_MIGRATE/TAU_FONT_REPACK/TAU_SDRAM_BUSY/TAU_BLIT) -- the combination that has never been fit before and whose absence caused B-130. Staged via git archive HEAD (clean tree, commit 6b75e89) into two directories, qsf's diffed to confirm only the SEED line differs. Found and fixed a real environment gap while launching: quartus_sh isn't on PATH in a non-interactive SSH session (needs an explicit export, not relied on from .bashrc/.profile). Both seed 1 and seed 2 quartus_map processes confirmed running independently (own cwd, survived a fresh SSH reconnect) -- result pending, typical 50 min-1h45m per seed. Once done: pick a seed, package as --semver 0.5.0-alpha.1 with SDRAM_BUSY=1 firmware, install for a corrected first hardware run.
- 2026-09-23 (Claude): docs/evidence (B-133). Owner (in the sibling Tau Omega session) pointed out this repo "should already have screenshots saved from constant tests." Checked: AUDIT_TRAIL records dozens of "hardware-observed... screenshot" lines but none of those images were ever copied into this repo. The mounted Pocket's own `Memories/Screenshots` folder still had 140 files back to 2026-09-20, never cleared. Matched 13 by timestamp to evidence lines B-058/B-060/B-062/B-067/B-069, re-decoded every QR page with tools/decode_tau_suite.py --qr and confirmed each matches what its cited entry already recorded. Copied into the sibling Tau Omega repo's testdata/screenshots/ (byte-verified against the card) with a provenance README, closing that project's "no real QR fixture" gap. No card write; SD card only read. Correction 2026-09-24: a duplicate local copy under work/diagnostics/check-qr-2026-09-21/ was removed -- the two repos should only ever reference each other's files, not hold literal copies; Tau Omega's testdata/screenshots/ is the one canonical copy.
- 2026-09-23 (Claude): docs only (B-137). Owner: "Log a question to think about how the library and cold file will work across tau version updates and if anything may break or need a migration." Added docs/MEDIA_LIBRARY_0.4_SPEC.md section 15: read the index version check, the root-string coupling B-136 exposed (safe for real releases since platform id doesn't change, narrower risk than B-136 suggested), the existing LIB_ID resume safety net (covers resume only), and the cold image's strict layout-id rejection (already well-mitigated by shipping ROM+cold together). Logged three genuinely open points -- schema-version forward compatibility never exercised, playlist/queue staleness after a re-sync not yet examined, no decision on forcing a re-scan on a version change -- as a question to revisit, not solved now. Cross-referenced from ARCHITECTURE_ROADMAP.md and CURRENT_STATUS.md. No code touched.
- 2026-09-23 (Claude): docs/tools (B-138). Owner: "tested with 3 modes, check card." Decoded the persisted Check summary (interact_persist.json ids 20-23, run 3/FULL): PASS on everything not gated on live playback (SDRAM/PSRAM window, cold code, library, timings, stress R1-R3, soak, cold x20); FAIL on track changes; neither pass nor fail (SR_NA/SKIP) for Playback counters (CT_AUD) and the new Blit storm (CT_BLT) -- both need music actually playing, and their absence from both lists means Check ran without a track playing, not that either errored. Found and fixed tools/decode_tau_suite.py's TESTS table stopping at id 12 (CT_BLT/id 13 had no name -- the range(15) fallback would have shown "test 13" if it had a result, so this wasn't hiding a real failure, but needed fixing for the next run). make test-host still passes. The corrected 0.5.0-alpha.1 build (B-134/B-135, library fix B-136) is solid on everything measured; the blit engine's actual hardware verdict is still unmeasured -- needs a re-run with a track playing. Track-changes failure unexplained, not yet investigated.
- 2026-09-23 (Claude): firmware/tools (B-139). Owner corrected B-138's guess ("check was running with music playing"), then asked to "add a check or visual indicator to the result to make sure we know if audio was playing and if it played the whole test." New chk_aud_full/chk_blt_full flags (fw/suite.inc): reset at arm, latched to 0 the instant a later poll sees playback stop/pause before the window closes -- previously CT_AUD/CT_BLT only checked once at entry, so a track ending or a pause mid-window was invisible and the verdict silently computed from partial data. Surfaced on screen (chk_line() appends * to the PASS/FAIL text for that row) and in the report (SR_T_AUDIO's always-0 word[1] now carries the flag; CT_BLT's value packs its own flag into bit 16, no format change needed). decode_tau_suite.py updated to decode both (audio entries now a named dict, CT_BLT gets busy_permille/audio_full unpacked); sim/test_suite.py's one affected assertion fixed, two new decode tests added. make test-host passes (20 checks); player-library-diagnostic-profile/player-library-diagnostic/player-library-check/release player all rebuilt clean, dist/ ROM confirmed byte-identical. Doesn't retroactively explain B-138's specific run (overwritten already) but closes the ambiguity for every future one.
- 2026-09-23 (Claude): card (B-140). Owner said "yes" to installing B-139's fix. Rebuilt player-library-diagnostic-profile with SDRAM_BUSY=1, repackaged with the same seed-1 RBF (no RTL change) as --semver 0.5.0-alpha.2. Backed up and removed the superseded TAU_0_5_0_A_1; installed TAU_0_5_0_A_2, hashes verified. This time built the library index correctly from the start (sync_media.py --from-core ... --library in one step, root confirmed correct) -- B-136's lesson applied, not repeated. Card ejected. Not yet run.
- 2026-09-23 (Claude): tools/card (B-141). Owner: "core not showing up on pocket. check update procedies." Compared TAU_0_5_0_A_2's files against working TAU/TAU_DEV_42 -- all present, data.json identical, core.json same shape. Two questions confirmed other cores still work fine after a restart+reinsert, isolating it to this core's own files. Found the one deviation: core.json's version field held "0.5.0-alpha.2" -- a format no core in this project's history has ever used (every prior core, release or TAU_DEV_NN, carries plain X.Y.Z); --semver (B-131) was the only feature to ever write something else there. Not confirmed against Analogue's own schema docs, but strong circumstantial evidence. Fixed package_sdram_stress.py to leave core.json's version as whatever dist/'s copy already carries; semver identity stays fully in shortname/description/platform id, which was always sufficient. Repackaged as TAU_0_5_0_A_3 (same ROM/RBF as -alpha.2), installed after backing up and removing -alpha.2, library index built correctly rooted. Owner separately noted the earlier Finder/Mac disruption was fskit, unrelated, resolved on their end -- noted as a reason to keep card operations lighter going forward. Not yet confirmed the core actually shows up.
- 2026-09-23 (Claude): tools/card (B-142). Owner: "not working. check skill or documentatio for properly release naming - the semver version is likely to be only possible in the name itself." Read the analogue-pocket-dev skill's json-files.md and sd-packaging-assets.md (targeted paths, no broad search, per the earlier fskit incident): description<=63 chars documented, and "cores also disappear from the openFPGA menu if json is invalid." TAU_0_5_0_A_3's description was 264 chars -- 4x the limit. Corrected B-141: the docs confirm version<=31 (SemVer) is the field's actual intended use including pre-release suffixes, so B-141's version-field theory wasn't supported by spec (kept anyway, harmless). Fixed package_sdram_stress.py to hard-truncate descriptions over 63 chars and write the full text to a new info.txt (the documented About-screen field this was always meant for) instead. Repackaged as TAU_0_5_0_A_4 (same ROM/RBF, description now 63 chars exactly), installed after backing up and removing -alpha.3. Not yet confirmed on hardware; if still missing, next step is Tools > Developer > Builds on the Pocket itself for a live diagnostic.
- 2026-09-23 (Claude): card (B-143). Owner: "still not showing. could it be the cache." Yes -- the Pocket's five catalog cache files (System/core_viewby_platform.bin, corelist_cache.bin, cores_cache.bin, platform_viewby_category.bin, platforms_cache.bin) were stale, predating every install this session did, and this session's entire B-129..B-142 sequence never cleared them -- despite this project's own CLAUDE.md section 3 rule literally naming "backup / clear-catalog / verify-SHA-256 / eject" as the standing card-write procedure. B-141's version-field theory and B-142's description-length fix may both have been correct-but-irrelevant given this was sitting in the project's own history the whole time. Backed up all five (verified byte-identical), deleted them so the Pocket rebuilds on next scan, ejected. TAU_0_5_0_A_4 left as-is. Not yet confirmed the core appears.
- 2026-09-23 (Claude): firmware (B-145). Owner: "on next build add an improvement to close the menu when pressing start so i dont have to navigate everything back." Settings already had this (set_input()'s existing Start->set_close() shortcut); the gap was the library browser (Start was silently discarded at any depth, requiring one B press per nested level) and the legacy playlist overlay (Start did nothing, only B closed). Added Start->close to both: lib_ui_input() now closes immediately from any depth (without resetting lib_depth, matching lib_request()'s existing browse-position-memory precedent), the playlist overlay's B handler now also fires on Start. make test-host passes; player-library-diagnostic-profile/player-library-diagnostic/player-library-check/release player all rebuilt clean. dist/'s ROM changed and committed -- this is a real product fix (library+legacy playlist are both in the shipped release), not diagnostic-only. Not applied to Check/Decode-Sweep pages, which already handle Start themselves for a documented reason. Not yet run on hardware.
- 2026-09-23 (Claude): docs (B-146 MILESTONE). Owner: "I had already run check and saved screenshots." Found and decoded 3 Check QR screenshots from the card (USER CHECK, STANDARD, FULL, all after the B-143 cache fix). Blit storm test PASSED on both STANDARD and FULL, SDRAM 15.8% busy over the window, audio confirmed continuous the whole 30s both times (B-139's audio_full flag true) -- zero late underruns, not ambiguous the way B-138 was. This is the real hardware proof PHASE_F_SPEC.md section 12.1 was built for: the blit engine is now correct in simulation (B-125), timing-closed on real hardware (B-134), and load-tested on real hardware with real audio (this result). Track changes still fails both runs, unexplained. Updated PHASE_F_SPEC.md section 12.1 and CURRENT_STATUS.md with the milestone.
- 2026-09-23 (Claude): docs only (B-147). Owner: "late, continue with blit engine." Read the real target (fw/meter_thumbs.h + set_draw_thumb(), 8-entry palette + RLE, one fb_rect per run today) before designing in the abstract. Wrote a concrete two-step B8 design into PHASE_F_SPEC.md: step 1 plain CLUT blit (opcode 7, 256x16 M10K CLUT via dedicated R_CLUT_IDX/DATA, reuses OP_BLIT's addressing); step 2 folds in B10 by reading meter_thumb_rle's exact byte format directly, deferred. Deliberately stopped before RTL: the CLUT's M10K read latency needs pipelining into A_COPYRD, the exact shared state OP_COPY/OP_BLIT depend on and that just passed its first real hardware load test tonight (B-146) -- judged worth review before modifying, not a solo late-night call. No code changed.
- 2026-09-23 (Claude): RTL/sim, no hardware (B-148). Owner: "do it" -- implement B-147's designed B8 step 1 (CLUT blit). New OP_CBLIT opcode (3'd7), a 256x16 CLUT (one M10K, dual clk_sys write/clk_sdram read) loaded via new R_CLUT_IDX/R_CLUT_DATA (mp3_soc.v, MMIO 0xC8/0xCC), new two-state dispatch (A_CBLIT_RD/A_CBLIT_WAIT) reusing OP_BLIT's addressing but one-word-per-transaction like OP_SBLIT -- deliberately never touching the shared A_COPYRD burst path OP_COPY/OP_BLIT depend on, the exact design choice flagged for review last night. Real bug found by simulation (not review): clut_raddr was first a register set the same edge it's needed, landing the CLUT's answer one cycle late (an 'x' in the very first test dump); fixed by making it combinational off p0_q directly. Verification: tb_blit_scene.v's scene gained a 12th command (real OP_CBLIT + a 5-entry CLUT), blit_reference.py gained cblit(), new mutation hook BUG_CBLIT_NO_LOOKUP caught. make test-rtl (full suite) passes with zero regression on the A_COPYRD/OP_COPY/OP_BLIT paths this design was built to leave untouched. make rtl-lint clean, make test-host unaffected. Not done: no Quartus fit, no firmware integration (player.c/set_draw_thumb() untouched) -- RTL+sim verification only, the real production win is still unrealized on hardware.
- 2026-09-23 (Claude): VM (B-149). Owner: "continue" -- following B-148's own next step. Launched the Quartus fit for B8 step 1 (CLUT blit), same proven G3+blit qsf combination B-134 already closed timing on (OP_CBLIT's RTL is unconditionally compiled in, no new macro needed, so this is a re-fit of a known-good config with a small addition, not a new experiment). Staged via git archive HEAD (commit 8c14754), confirmed the new CLUT code present in both trees, qsf diffed to confirm only SEED differs, both seeds confirmed running independently. Result pending.
- 2026-09-23 (Claude): docs only ([TO], Tau Omega's own log, not this file's B-series). In the sibling Tau Omega session: built tau_core::taud there, a byte-exact Rust port of this repo's own tools/decode_tau_suite.py (parse_record/from_text) plus a PNG+QR-grid decode pipeline, verified against 7 of B-133's recovered real screenshots (cross-checked against decode_tau_suite.py --qr on the same files). Nothing in this repo changed; logged here per section 3 since it consumes this repo's fixtures and reference decoder.
- 2026-09-23 (Claude): card ([TO], Tau Omega's own log, not this file's B-series). Owner ("go ahead") approved validating the sibling Tau Omega project's sync/package-install/remove write paths on the real mounted Pocket card. SHA-256-verified TAU/TAU_DIAGNOSTIC/shared common files before and after every stage. Sync: 3 real MP3s into a new Assets/tauomegasync/common. Package install: the real 0.4.0 release zip repackaged under a fully separate namespace (tauomegapkg/alfatreze.TAU_OMEGA_PKG, zero overlap with real content) and installed via a throwaway scratch binary. Remove: removed the test core cleanly (solo/non-shared case); found one real, harmless edge case (Finder AppleDouble junk defeats the folder-collapse optimization, correctly falls back to per-subfolder deletes instead of guessing). All scratch namespaces cleaned up, card ejected. Caught and fixed my own mistake mid-task: a scratch script got written to this repo's own src/main.rs instead of the scratchpad (a relative path used after a cwd reset) -- caught immediately via git status, deleted before ever staged, confirmed clean. Full writeup in Tau Omega's docs/STATUS_HANDOFF.md ("Real hardware write validation"); no longer cross-referenced by a B-NNN id here (2026-09-24: this collided with this file's own later B-series entries and Tau Omega work was moved out of this file's B-series entirely, see AUDIT_TRAIL.md's note above B-150).
- 2026-09-24 (Claude): docs only ([TO], Tau Omega's own log, not this file's B-series). In the sibling Tau Omega session: built tau_core::screenshots there (list_screenshots over Memories/Screenshots/, the location B-133 found on this repo's own card), newest first, parses the Pocket's YYYYMMDD_HHMMSS naming, skips Finder junk. Wired into Settings as a "Browse screenshots on a card" section feeding the existing QR-decode card. 3 new tests against testdata/screenshots/. Nothing in this repo changed; logged per section 3 since it reuses this repo's own hardware finding and fixtures.
- 2026-09-24 (Claude): docs (B-150). Owner: "check" / "Try again" on the B8 Quartus fit. Both seeds Successful (RAM 299/308, exactly +1 for the new CLUT M10K, no surprise usage) but timing did NOT close on either: seed 1 Slow 85C -0.086ns/Slow 0C -0.196ns setup; seed 2 (better) Slow 85C +0.088ns/Slow 0C -0.025ns. Hold clean on both. A real, small, consistent-direction regression from the CLUT's physical cost, not seed noise. Attempted to trace the exact violating path via quartus_sta/report_timing but hit a wrong TCL API call (get_paths isn't valid) and didn't retry with the correct one -- not chased further tonight. This bitstream is NOT ready to ship; the working TAU_0_5_0_A_N line (pre-B8) is unaffected. Updated PHASE_F_SPEC.md and CURRENT_STATUS.md with the honest result and the next step (find the actual path before attempting a fix).
- 2026-09-24 (Claude): docs (B-151). Owner: "continue" -- found B-150's actual violating path this time (fixed the wrong Tcl API call from before). Worst setup paths all run from mp3_fb.sv's internal muxes into glyphbuf's own register inputs -- the exact same shared write-data selection network this whole blit-engine phase has hit marginal three times now (B-109's original MLAB-era violation, B-111's BAR retiming, B-116's TAU_BLIT_BLEND congestion finding), for three different reasons, all converging on this one physical bottleneck. OP_CBLIT is a fourth competing write source into it. On reflection, flagged that my first-cut hypothesis (the BUG_CBLIT_NO_LOOKUP mutation ternary specifically) is the less likely explanation by analogy -- BUG_BLEND_ALWAYS_SRC is a structurally similar ternary already in the same write path and hasn't caused a new violation on its own -- so the more likely cause is simply having a fourth mutually-exclusive write source, which is a real design question (does Quartus fail to merge mutually-exclusive case-arm writes efficiently here?) not a one-line fix. Did not launch another guess-based re-fit tonight given genuine uncertainty about which hypothesis is right. Updated docs/AUDIT_TRAIL.md B-151 correcting B-150's "not identified" note.
- 2026-09-24 (Claude): RTL/sim (B-152). Owner: "yes" -- try the cheap experiment before assuming a real retiming fix. Replaced the inline BUG_CBLIT_NO_LOOKUP ternary at the glyphbuf write site with a generate-gated wire (cblit_wval): the alternate branch literally doesn't exist in the netlist when off (elaboration-time guarantee), not reliance on Quartus folding a parameter-constant ternary -- never actually verified for any mutation hook in this file before. make rtl-lint clean, full make test-rtl passes, mutation still correctly caught. Not yet known if this recovers B-150's lost margin -- launching the re-fit next.
- 2026-09-24 (Claude): docs (B-154). B-153's re-fit finished: timing slack bit-for-bit identical to B-150's original (Slow 85C +0.088ns, Slow 0C -0.025ns) -- the generate restructuring made zero difference, conclusively ruling out the mutation-ternary hypothesis. Confirmed: simply adding OP_CBLIT as a fourth write source into glyphbuf's shared network costs the margin, independent of how its value is computed. Next: a real retiming fix (B-111/B-114's proven technique), not attempted yet.
- 2026-09-24 (Claude): full session arc, B-156 through B-178. B8 (CLUT blit) closed the retiming fix (B-158/B-159, first-attempt clean fit), firmware-integrated (B-160), a real boot-hang bug found and fixed (B-162), and proven on real hardware under sustained load with real audio (B-164) -- done. Fixed two cross-project boundary violations with the sibling Tau Omega repo (an ID-counter collision, a duplicated fixture file) and set a standing rule: the two repos never share literal files, only reference each other's (B-156). Instrumented the pre-existing, unrelated `Track changes` Check failure to report why, not just that it fails (B-165). Built a new "Blit Test" Diagnostics page exercising every draw-engine opcode individually, full screen, no audio needed (B-166) -- three opcodes (OP_RUN/OP_BAR/OP_SBLIT) had never been issued from firmware before at all. It hangs on real hardware; four source-level fix attempts (an ordering bug, a stride-addressing rewrite, draw-based checkpoints, memory-only crumbs) have NOT resolved it, and the last attempt made it worse in a way hard to explain from source alone. Built and fit-proved ISSP (In-System Sources and Probes) for live JTAG readback of the draw engine's state (B-172/B-173), and worked out + verified its real readback procedure against the live system-console tool while waiting for cable access (B-178) -- found the real tool location and Tcl command names (system-console is NOT in quartus/bin; the service type is `issp` not `source_probe`), recorded as skill KB-057. Full handoff: docs/SESSION_HANDOFF_2026-09-24_BLIT_TEST.md. Card left as-is (TAU_0_5_0_A_12, the hanging build) per owner instruction -- do not reinstall until ISSP gives a real diagnosis.
- 2026-09-24 (Claude): RTL/sim (B-179). No JTAG cable yet for the Blit Test hang (docs/SESSION_HANDOFF_2026-09-24_BLIT_TEST.md); owner declined another blind source-level fix and a card write, chose Tier 2 blit work instead. Built B9 (palette re-index): an 8-bit sticky offset (blt_reindex, section 9 field 6) added to OP_CBLIT's palette index before the CLUT lookup -- "re-index, don't blend," reusing B8's own CLUT read port, one adder, no new state, default 0 a true no-op. Verified: tb_blit_scene.v's 13th scene command (a reindexed CBLIT through a separate CLUT bank), blit_reference.py's cblit() gained a matching parameter, new mutation hook BUG_IGNORE_REINDEX caught. make rtl-lint/test-rtl (full suite)/test-host all pass, 0 failures, zero regression. Not done: Quartus fit, firmware integration. docs/MMIO_ALLOCATION.md, docs/PHASE_F_SPEC.md section 5, docs/CURRENT_STATUS.md updated. Card/VM untouched.
- 2026-09-24 (Claude): docs only (B-180). Analysed B11 (hardware rounded-rect) after B9: cmd_op is already full (all 8 3-bit values taken by existing opcodes), so a new opcode needs it widened to 4 bits, touching the shared R_FB_GO/cmd_op decode path; the corner-cut geometry (iterative quarter-circle search) should not be computed live in RTL per this session's own repeated timing lesson (B-109/B-111/B-150/B-157) -- proposed loading a small precomputed cut-per-row table via a new sticky MMIO pair instead, same idea B8's CLUT already used. Wrote the full design into docs/PHASE_F_SPEC.md section 5 (field reuse, dispatch shape, verification plan) -- precise enough to build cold. Owner chose design-only over a full build this pass. No RTL/MMIO/testbench changes. docs/CURRENT_STATUS.md updated.
- 2026-09-24 (Claude): docs only (B-181). Analysed B10 (hardware RLE source blit) after B11's design. Read the real target (fw/settingsui.inc's set_draw_thumb()/set_thumb_flat_build()) before designing and found B10's original motivation is already substantially met by B8 step 1 (shipped, hardware-proven): RLE decode already happens once per session (~16ms), not per draw. Flagged this to the owner before building; B10 would only save that one-time cost plus ~38.5KB of SDRAM (not the scarce M10K resource). Owner chose design-only for completeness. Wrote the design into docs/PHASE_F_SPEC.md section 5 (chained OP_RECT-shape bursts of clut[idx] per decoded run, shares B11's cmd_op-widening need, flags a genuinely new data-dependent source-consumption rate needing its own mutation-test design). No RTL/MMIO/testbench changes. docs/CURRENT_STATUS.md updated. All three Tier 2 items (B9 built, B10/B11 designed) now at least analysed.
- 2026-09-24 (Claude): docs only (B-182). Owner asked if it's safe to move ahead with the M10K/RAM-shrink track. Confirmed the real gate (PHASE_F_SPEC.md section 4): meters must go cold, which needs ui_draw_dynamic rewritten to use the blit engine first. Scoped the first step (plain-bars mode already matches OP_BAR's convention, fb_bar() already exists) but found enabling it means calling blit_probe_ensure() -- the exact function implicated in the still-unresolved Blit Test hang -- from the live playback path, a materially worse risk than the current diagnostics-page hang. Owner chose to hold the entire track until ISSP gives a real diagnosis. Design recorded in docs/PHASE_F_SPEC.md section 4, ready to build once unblocked. No code changes.
- 2026-09-24 (Claude): docs only (B-183). Owner asked to review HarpMudd upstream's v1.5.0 update for port/drop candidates against current features and roadmap. Fetched upstream, verified against real diffs. Corrected a scope error from B-102: Tau's fork point is right before v1.5.0 (git merge-base), not an early baseline -- a prior note had conflated Tau's own v0.3.0 tag with upstream's identically-named one, so none of v1.5.0 had actually reached Tau. Also found an active release/1.5.1 branch with hardware-measured work toward v1.6.0. Wrote docs/HARPMUDD_UPSTREAM_1.5_REVIEW.md: evidence-graded PORT/CONSIDER/DROP table. Top findings: clk_sys 60->66.667 MHz (release/1.5.1, +11.1% CPU headroom, same PLL/VCO constraint Tau shares, confirmed both of upstream's silent-failure hardcode traps exist verbatim in Tau's mp3_soc.v -- needs its own Quartus experiment against Tau's blit-engine timing margins before trusting); PCM FIFO start-of-track cushion (v1.5.0, hardware-confirmed fix, Tau's pcm_fifo.v untouched since the fork -- clean low-risk port); meters-yield-to-audio (release/1.5.1, firmware-only, zero dependency on blit_probe_ensure() -- buildable now, in parallel with the JTAG wait, complements the held B-182 meter/blit integration). Smaller items: a real UTF-8-vs-Windows-1252 tag-encoding gap, independent confirmation the CJK font belongs in SDRAM not on-chip. Dropped several items as architecture-mismatched, aesthetic-only, or superseded by Tau's own more thorough work. docs/CURRENT_STATUS.md updated. No code/card/VM touched -- evaluation only.
- 2026-09-24 (Claude): RTL/firmware (B-184). Owner asked to port four items from the HarpMudd upstream review (PCM FIFO cushion, meters-yield-to-audio, UTF-8/Windows-1252 tag guess, cassette meter), park the clk_sys bump. Built all four. Real size problem found and fixed: the combined ~3.6KB overflowed the bare `player` target (make firmware's own build) -- gated the cassette's heavy code behind TAU_METER_THUMBS (matching the thumbnail system's own "extras" flag) plus a VIZ_CYCLE_COUNT excluding it from the cycle button/settings list on builds without the macro; verified every named build target still links (player, release, player-diagnostic, player-library-diagnostic-profile, player-library-check). Found and fixed a real, otherwise-undetected regression: the pre-existing sim/tb_pcm_decay.v hangs forever with the priming cushion in place (confirmed the identical bug exists unfixed in upstream's own release/1.5.1 too, so latent not introduced). make rtl-lint/test-rtl/test-host all pass, 0 failures. Not installed on the card; no Quartus fit run yet. docs/AUDIT_TRAIL.md B-184, docs/CURRENT_STATUS.md, docs/HARPMUDD_UPSTREAM_1.5_REVIEW.md updated.
- 2026-09-24 (Claude): card (B-185). Installed TAU_DEV_44 with the three firmware-only HarpMudd-ported items (meters-yield-to-audio, UTF-8/Windows-1252 tags, cassette meter) for testing; the PCM FIFO cushion needs a Quartus fit first, not yet launched, so it's not on this build. Followed docs/CARD_INSTALL_PROCEDURE.md in full: backed up/verified caches and media before touching anything, SHA-256-verified bitstream/ROM/cold-image, rebuilt the library index correctly rooted (sync_media.py --from-core), cleared the five catalog caches, ejected. Installed additively -- TAU/TAU_DIAGNOSTIC/TAU_DEV_42/TAU_0_5_0_A_12 all untouched. docs/AUDIT_TRAIL.md B-185.
- 2026-09-24 (Claude): MILESTONE (B-186). JTAG cable access arrived; walked owner through connectivity (UTM USB passthrough needed re-attaching, then the cable needed reseating at the Pocket end). Loaded the fit-proven ISSP debug bitstream via quartus_pgm over JTAG (no SD write), owner reproduced the Blit Test hang fresh on it, read the probe live. Found and fixed a real tool-syntax bug in the documented procedure (B-178's open_service silently returns an empty handle once a cable is actually connected; claim_service is the working form -- corrected docs/JTAG_DEBUG_ACCESS.md). Result: the draw engine's dispatch state machine is NOT hung -- 12 reads over ~2.5s showed only routine idle/scanline-fill cycling, empty command FIFO, no opcode in flight. This rules out every RTL/dispatch-state hypothesis this thread has run since B-166 and means the hang is CPU/firmware-side. docs/AUDIT_TRAIL.md B-186, docs/JTAG_DEBUG_ACCESS.md section 6.5, docs/CURRENT_STATUS.md updated. Next: a firmware-side trace, not another RTL fix.
- 2026-09-24 (Claude): RTL/VM (B-187). Built the CPU-side follow-up to B-186's finding: a plain MMIO checkpoint register (R_DBG_MARK) that bt_crumb() now also writes, wired to a second ISSP probe instance (DBGM) readable live over JTAG regardless of CPU state -- same property that made the BLIT probe useful. make rtl-lint/test-rtl-fb/test-host pass, firmware builds clean. Committed (86d8e85), staged to the VM, launched the Quartus fit (seed 2, single seed, debug-only bring-up). Running in the background, ~50min-1h45m typical. Next once done: load via JTAG, reproduce the hang, read DBGM for the exact last-reached checkpoint. docs/AUDIT_TRAIL.md B-187.
- 2026-09-24 (Claude): MILESTONE (B-188), **CORRECTED (B-189)**. B-188's DBGM=0x00 read is unconfirmed and likely invalid: `R_DBG_MARK` was only added in commit `86d8e85` (B-187), but the firmware actually running on `TAU_0_5_0_A_12` (ROM `8daac175...`, confirmed by reading the card) was packaged in B-177, before that register existed -- so `DBGM=0x00` may simply mean "this firmware never writes that register at all," not "bt_begin() never executes." B-188's "bt_begin() never starts" conclusion does not stand on the evidence collected. Fixed the process gap: loading a new bitstream via JTAG reprograms the FPGA only, never the SD card's firmware -- the two must be kept in sync by hand for every ISSP debug iteration. Rebuilt `player-library-diagnostic-profile` from current `fw/suite.inc` (ROM `05565491d5...`, confirmed `bt_begin` is `COLD_FN` at word address `0x09202693`) and reinstalled it onto `TAU_0_5_0_A_12` on the card (backup verified byte-identical before/after, same RBF, media untouched, no catalog rebuild needed since this is an in-place ROM/cold-image swap on an existing core). The DBGM (and IFPS) reads need to be redone against this corrected firmware before trusting any conclusion about where the hang actually is. docs/AUDIT_TRAIL.md B-189, docs/CURRENT_STATUS.md, docs/JTAG_DEBUG_ACCESS.md to be updated to flag B-188 as unconfirmed.
- 2026-09-24 (Claude): RTL/VM/docs (B-190). Owner reloaded `TAU_0_5_0_A_12` through the Pocket's own core menu (with B-189's corrected firmware) and reproduced the hang; found a second real process gap doing the re-read -- loading a core through the Pocket's UI reprograms the FPGA fresh from the SD card's own (pre-ISSP) `.rbf`, silently overwriting the JTAG-loaded DBGM/IFPS debug bitstream (`get_service_paths issp` came back empty). Reloaded the ISSP `.sof` via `quartus_pgm -m jtag`, had the owner reproduce the hang again with plain buttons only. Found and fixed a second real tool bug: `issp_read_probe_data -instance $path` (used in this session's later scripts) silently returns the literal string `"error"` with no exception -- the correct form, confirmed via `help issp_read_probe_data`, is positional (`issp_read_probe_data $path`). Clean result, 12 reads over ~2.4s: **`DBGM = 0x00` constant** (now real evidence -- `bt_begin()`'s first line genuinely never executes), **`IFPS = 0x44` constant** (`gi` and `i_is_psram` set, every live-transaction bit -- `busy`/`ictl_req`/`dctl_req`/`iCYC`/`iSTB` -- zero), `BLIT` matching B-186's idle/fill pattern. This weakens rather than confirms B-188's PSRAM-fetch-stall hypothesis: an actively-stalled fetch would show a held request, not an idle arbiter. The more consistent picture: something upstream of the `bt_begin()` call site, in ordinary on-chip code, holds the CPU first, with no instruction-fetch bus activity at all while stuck. Next step is the already-committed PCAD probe (`iADR`/`dADR`, `3527cde`) -- needs its own Quartus fit, not yet launched. docs/AUDIT_TRAIL.md B-190, docs/CURRENT_STATUS.md, docs/JTAG_DEBUG_ACCESS.md section 6 updated with both tool/process gaps.
- 2026-09-25 (Claude): MILESTONE (B-191). Built and fit the PCAD probe (0 negative slack, 0 errors), loaded via JTAG, owner reproduced the hang, read all four probes. `iCYC=0`/`iSTB=0` (idle instruction fetch, matches B-190) but `dCYC=1`/`dSTB=1`/`dACK=0` -- a genuinely live, permanently unacknowledged DATA transaction. `iADR` sits inside `bt_draw()`; disassembling that exact PC found the CPU frozen on `lhu s0,964(a5) # a00003c4` -- `bt_crumb_read()`'s own SDRAM halfword readback (964 = BT_CRUMB_ADDR<<1), called from `bt_draw()`'s idle-screen redraw, before the user's Start press ever reaches `bt_begin()`. This is the actual root cause of the whole B-166..B-190 hang investigation: the checkpoint mechanism B-176 built specifically to safely observe a hang, without risk of hanging itself, is the thing that hangs. Leading hypothesis for why (unconfirmed): `blit_probe()` uses the identical raw-pointer SDRAM-window access and works, but only after `blit_probe_ensure()` has fired at least once elsewhere (`bt_begin()` or a meter-preview thumbnail draw in Settings) -- `bt_crumb_read()` may be hitting this window completely cold, first-access-of-boot. Proposed a free, zero-engineering test to the owner (visit a meter-preview screen first, then Blit Test) before any firmware change. docs/AUDIT_TRAIL.md B-191, docs/CURRENT_STATUS.md updated.
- 2026-09-25 (Claude): docs/A-B-test (B-192). Owner suspected the HarpMudd port; A/B tested pre-HarpMudd firmware (still hangs) and the real shipped non-ISSP bitstream (still hangs), then tested "warm the window with a proven-safe access first" (Window Test PASS, then meter-preview list: still hangs). All three alternative theories ruled out. Root cause fully confirmed: `0xA0000000`-relative raw CPU pointers hang unconditionally on every firmware/bitstream combination; `PL_SDRAM_BASE`(`0xA0100000`)-relative ones work every time. Real, pre-existing, unconditional bug, not caused by anything built this session. docs/AUDIT_TRAIL.md B-192.
- 2026-09-25 (Claude): firmware (B-193). Fixed all three functions using the broken `0xA0000000` idiom. `bt_crumb()`/`bt_crumb_read()` relocated onto `PL_SDRAM_BASE+0x10000` (pure CPU scratch, safe to move) -- confirmed fixed on hardware, Blit Test idle screen no longer hangs. `blit_probe()` rewritten to use the SDRAM mailbox instead (its sentinel can't move -- `fb_cmd_addr` is hard-capped at 19 bits, the draw engine can never reach `PL_SDRAM_BASE`); found and fixed two bugs along the way, an unverified partial byte-enable assumption (caused a false "not detected") and a genuine cross-client race between the draw engine's write and the CPU mailbox's readback (fixed with a 512-cycle settling delay) -- confirmed `BLIT_READY()` now passes and `bt_begin()` completes into `bt_advance()`'s normal tick loop. `set_thumb_flat_build()` (same bug, found proactively before it was hit standalone) rewritten to use `fb_rect()` draw-engine commands per RLE run instead of ~21,504 individual raw-pointer writes. All three verified with make test-host and confirmed every firmware target still builds; installed and hardware-tested incrementally (5 card writes, each backed up/verified). docs/AUDIT_TRAIL.md B-193.
- 2026-09-25 (Claude): docs, session paused (B-194). Past all three B-193 fixes, the Blit Test itself now runs far longer than its fixed ~48s design duration without completing, hanging on one instruction, or resetting -- live-monitored 5+ minutes (JTAG chain never broke, PC kept moving, but the CPU-side checkpoint read exactly the same value the entire time, suggesting `bt_advance()` isn't being freshly re-entered as a new tick even though something is still executing). Owner: "we might well be here hours" -- stopped live testing here rather than continue waiting. Needs a new ISSP probe (`bt_op`/`bt_lvl`/`bt_at`/`bt_ops_done`) to pin down precisely, not yet built. Uncommitted at session end: `fw/blit_probe.inc`, `fw/settingsui.inc`, `fw/suite.inc` (B-193's three fixes), `dist/Assets/tau/common/{tau.rom,tau-cold.bin}` (release rebuilt clean with the same fixes). Card has the latest build on `TAU_0_5_0_A_12`; other cores untouched. docs/AUDIT_TRAIL.md B-194, docs/CURRENT_STATUS.md updated.
