# PHASE_F_SPEC.md section 10, step 2 -- the real multi-seed fit, all of Tier 1 bundled
# together as the section originally specified: MLAB migration + font repack (already
# individually fit-proven in B-101/B-102) + the SDRAM busy-cycle counter (B7) + the full
# blit engine (B1/B2/B4/B5/B6, B-103..B-106) including its separable TAU_BLIT_BLEND macro.
# This is the first real Quartus spend on the blit opcodes themselves -- everything before
# this was RTL/simulation only. Watch specifically for the documented -1.888 ns timing-cliff
# risk; if timing fails, the first bisect is dropping TAU_BLIT_BLEND alone (section 10).
# Appended to the STAGED src/fpga/ap_core.qsf, after any base macro set + a SEED line.
set_global_assignment -name VERILOG_MACRO "TAU_MLAB_MIGRATE=1"
set_global_assignment -name VERILOG_MACRO "TAU_FONT_REPACK=1"
set_global_assignment -name VERILOG_MACRO "TAU_SDRAM_BUSY=1"
set_global_assignment -name VERILOG_MACRO "TAU_BLIT=1"
set_global_assignment -name VERILOG_MACRO "TAU_BLIT_BLEND=1"
